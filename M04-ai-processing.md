# M04 — AI 处理管线

## 两段式架构

```
Stage 1: ASR（语音转文字）
  输入：视频音轨
  输出：带时间戳的原始转录文本
  
Stage 2: LLM 结构化（单次调用）
  输入：Stage 1 的完整转录文本
  输出：结构化 JSON（段落树 + 类型 + 标题 + 翻译）
```

## Stage 1 — ASR

### 三档选择（用户选具体模型）

| 档位 | 说明 | 模型选择 |
|------|------|---------|
| 字幕 | 使用视频已有字幕（YouTube/B站自动字幕、本地 .srt/.vtt） | 无需选模型 |
| API | 云端 ASR API | 用户选供应商（国内供应商 + 自定义供应商） |
| 本地Whisper | 本地运行的 Whisper 模型 | 用户选模型大小（tiny/base/small/medium/large-v3） |

### 优先级

字幕 > API > 本地Whisper（按成本从低到高）

- 若在线视频有字幕 → 直接用字幕，跳过 ASR
- 若无字幕 → 按用户配置的档位处理

### 语言检测（决策85）

三档 ASR **纯自动检测**语言，不需用户选、不可手动覆盖：

| 档 | 检测方式 |
|----|---------|
| 字幕 | YouTube/B站字幕轨自带语言标签；本地 .srt 按内容判断 |
| API | 供应商须支持自动检测（M19 准入约束） |
| 本地 Whisper | 内置语言检测（前 30 秒自动识别，准确率高） |

- ASR 跑完自动填 `Video.language`（'zh' / 'en' / 'other'）
- `language='en'` → Stage 2 自动生成 `translation`（决策33）；`zh`/`other` → 不翻译
- 多语言混杂视频：v1 不做特殊处理，按检测到的主语言走
- M19 准入加约束：ASR API 供应商须支持自动语言检测

### ASR 输出标准化为 Sentence[]

各档 ASR 原始输出粒度不同，统一标准化为 M02 的 `Sentence[]`（带 id/text/时间戳），再交给 Stage 2 / M18 / M07：

| 档 | 原始粒度 | 标准化方式 |
|----|---------|-----------|
| 字幕 | YouTube/B站字幕 2-5 秒碎片 | 合并碎片 + 按句末标点切成句 |
| API（讯飞/阿里/腾讯） | 多数原生给句级 | 基本直接用，个别需对齐 |
| 本地 Whisper | 段级/词级 | 开词级时间戳 + 按句末标点分组 → 句级 |

> M07 原文「ASR 天然给出句级时间戳，直接映射」修正为「由 ASR 段/词级时间戳后处理映射为句级」。

### 长音频处理

- **本地 Whisper**：内部 30 秒 VAD 自切、自动拼时间戳，原生处理长音频，无硬性时长上限；GPU 下速度可接受。
- **API 档**：用支持长音频的国产供应商（讯飞长语音/阿里录音文件识别/腾讯等），零切块。
- **不建 ASR 音频切块兜底（永久）**：不支持的供应商（如 OpenAI Whisper API ~30 分钟上限）用于长视频直接失败报错，不兜底。
- M19 准入约束：API 供应商必须返回句级时间戳（否则 M07 双击跳转失效）。

## Stage 2 — LLM 结构化

### 单次调用做全部工作

一次 LLM 调用，输入完整转录文本，输出结构化 JSON：

| 任务 | 说明 |
|------|------|
| 分段 | 将转录文本切成段落，各有起止时间 |
| 分类 | 给每个段落打类型：概念描述/例子/类比/过渡 |
| 命名 | 给每个段落/小节/章节生成标题 |
| 建层 | 构建 章节>小节>段落 的树形结构（默认 3 层） |
| 翻译 | 若转录文本含英文，翻译成中文，存入段落 `translation` 字段（原文+翻译并存，不替换 text/sentences） |
| 时间戳 | 保留句子级时间映射（从 ASR 时间戳映射） |

### 不做的事

- **不清洗文本**：保留原始转录（口癖、重复、语法问题均保留）
- **不表达 LaTeX/代码**：文本不含公式渲染和代码块，用户看视频区
- **不做画面理解**：管线不看视频画面，仅处理音轨转录

### 输出格式

```json
{
  "chapters": [
    {
      "title": "章节标题",
      "start": 0,
      "end": 300,
      "sections": [
        {
          "title": "小节标题",
          "start": 0,
          "end": 120,
          "paragraphs": [
            {
              "title": "段落标题",
              "type": "concept",
              "start": 0,
              "end": 30,
              "translation": "中文翻译（仅英文视频有）",
              "sentences": [
                { "id": "s1", "text": "句子1", "start": 0, "end": 5 },
                { "id": "s2", "text": "句子2", "start": 5, "end": 10 }
              ]
            }
          ]
        }
      ]
    }
  ]
}
```

### 用 JSON Schema 约束输出

使用 function calling / structured output 降低格式出错率。

### Path B：输出不存 text 字段

- LLM 输出**只存 `sentences`**（每句带 id/text/时间戳），**不输出 `text` 字段**。
- app 把 `sentences` 拼接成 `text` 入库（M02 的 `Paragraph.text` 仍保留，由拼接得出）。
- 理由：`text` 与 `sentences` 重复存原文，去掉后输出 ≈ 输入 1.5~2 倍（原 2~3 倍），省 token、更快更省。
- 副作用（正向）：LLM 只能分组句子、不能改写文本，**结构上锁死"不清洗"**（决策5）；消除 text/sentences 不一致隐患；M18 合并、决策15 拆分都以句子为原子，更干净。
- 唯一实现注意：句子拼接保真（英文按空格连、中文直连，ASR 通常自带句内标点），测试时验证。

## 长视频处理

- 长视频挑战分两层：Stage 1 ASR 音频太长（见上"长音频处理"）、Stage 2 文本太长（见 M18）。
- Stage 2 文本分块方案见 **M18**（M18 是 Stage 2 的包装层：分块→每块 Stage 2→合并）。
- 触发条件：转录文本 token > 结构化 LLM 窗口 × 33%（Path B 后统一值，详见 M18）。
- 短视频走单次调用，不触发 M18。

## 使用的模型

| 阶段 | 模型类型 | 说明 |
|------|---------|------|
| ASR | 专用 ASR 模型 | 用户自选（见 M19） |
| 结构化 | 通用 LLM（纯文本） | 用户自选（见 M19），不需要 vision |
