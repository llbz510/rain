# M17 — 视频列表与管理

> 状态：已确认（v4.4，第七次会话 2026-06-25）

## 已知设计

- 视频列表使用**原视频缩略图**作为卡片（不做统计封面，M12 已删除）
- 显示标题 + 缩略图
- 点击进入学习界面

## 待讨论（已全部确认，见下"已确认决策"）

- [x] 列表排序方式 → 决策58（默认最近学习，三档可切换，无手动排序）
- [x] 视频分组/标签 → 决策59（不做筛选/facet/标签/文件夹）
- [x] 搜索功能 → 决策59（关键词搜标题；转录稿搜索 v2）
- [x] 删除/导出视频 → 决策60（删除：硬删+强确认+保留本地文件+单个删）/ 决策61（导出：v1 不做）
- [x] 视频处理状态展示 → 决策55（卡片粗状态+百分比，导入框详细进度）
- [x] 多视频管理 → 决策54（多标签栏随 M11 进 v2）

---

## 已确认决策（第七次会话，2026-06-25）

### 决策54 导航模型 = 独立主页 / 单视频 / 多标签栏 v2
- App 打开→视频列表（满屏主页）；点视频→进入学习界面（替换列表）；学习界面"返回"→回列表。**同一时间只开一个视频**。
- 多标签栏（多视频同时开）随 M11 进 v2，不在 v1 做。
- 理由：M16 三模式均为单视频四区，无多视频区域；多标签栏 M11 明定 v2。

### 决策55 导入生命周期 + 状态粒度
- 导入一开始即建 Video 记录（status=pending），**立即作为卡片出现在列表**；卡片显粗状态 + 进度百分比，live 更新。
- M30"导入框"= 详细进度（stage/blockCurrent/blockTotal/percent/retrying），可关闭/最小化；视频**后台继续处理**，列表卡 live 更新，完成自动转 ready。失败→卡片显 failed + 重试入口。
- **状态粒度分工**：卡片=粗状态+百分比（轻）；导入框=M18 详细 progress（重）。同源 store。
- **并发导入**：v1 一次只导一个，其余排队（ASR/本地Whisper 吃 GPU、LLM 烧钱；排队简单）。
- 列表显示所有状态视频（pending/processing/ready/failed），非仅 ready。

### 决策56 观看进度数据模型 + M05 ■/□ 语义澄清（跨 M02/M05/M17）
- **M05 目录■/□ = 瞬时当前播放位置指示**（非持久）：当前播放时间之前=■、所在节点=当前、之后=□；拖动播放头回退→重算（■ 可撤回），不跨会话记忆。澄清决策 11/12 原含糊措辞。
- **Video 新增 2 字段**（改 M02）：`position`（最远到达秒数，持久单调，用于列表进度条 + 重开自动续播）+ `lastStudiedAt`（最近学习时间，用于"最近学习"排序）。
- **重开视频自动续播到 position**（不弹"继续/从头"，直接跳）。续播后当前播放时间=position，瞬时■/□ 自然显示到 position。
- **position-based（一个数），不上逐句 played**：逐句过重；position 线性观看准确，往前拖越过未看会误标（可接受小瑕疵）。
- 列表进度条（position/duration）与 M05 ■/□（瞬时）两套数据源、两处显示，靠"自动续播"桥接。

### 决策57 ready 卡片内容 + 网格形式
- 形式：缩略图网格（卡片=缩略图主视觉 + 底部信息条）。v1 只做网格，不做列表/网格切换。
- ready 卡显示：缩略图 / 标题 / 底部细进度条(position/duration)+时长 / 最近学习小字(2小时前·未看过) / 角标(语言中英·来源本地📁·在线🔗) / 已看完(position≈duration)→进度条满+淡✓
- 非 ready 卡（决策55）：缩略图加蒙层 + 状态徽章（处理中带百分比·失败带重试·排队中）
- 视觉细节（尺寸/比例/深浅色）归 M13

### 决策58 列表排序
- 默认 = 最近学习（lastStudiedAt 降序）
- 三档可切换：最近学习 / 导入时间 / 名称
- 自定义手动排序 = 不做（不进计划）
- （一度讨论"只按导入时间+倒序按钮"，经核对以本决策为准）

### 决策59 检索：无筛选 + 标题搜索
- 不做任何筛选/facet/标签/文件夹（"导入时间筛选"经澄清实为排序，归决策58）
- 搜索：关键词 → 列出标题包含该关键词的视频
- 转录稿搜索（跨视频内容搜索）= v2（与已有决策一致）
- 后果：无状态筛选 → 失败/处理中视频与 ready 混排，靠卡片徽章区分（决策55）

### 决策60 删除视频
- 删整段视频**不算违反"永不删除内容"**（原则防的是编辑时意外丢内容；删视频是显式主动动作）
- **仅硬删，不做软删/回收站**（含 v2）
- 强确认弹窗："将永久删除：视频 + N 段结构 + M 条笔记，不可恢复"
- 级联：视频 + 段落树 + sentences + notes 全删（v1 无派生引用，干净无悬空）
- 本地视频文件：**保留**（只删 DB 记录 + 缩略图，用户自管文件）；在线 URL 视频删 DB + 缓存
- 入口：卡片 ⋯/右键 → 删除；**v1 单个删，不做多选批量删**

### 决策61 导出 — 暂不做
- v1 不做导出（学习数据 / 视频文件均不导出），先搁置
- stub 原"删除/导出视频"中的导出项据此延后

### 决策62 M17 收尾交互
- 点非 ready 卡（处理中/排队中/失败）→ **重新打开该视频的导入框**（看详细进度 / 到失败重试处）
- 列表顶栏：`排序选择器` + `搜索框` + `导入按钮`，就这三样（位置/样式归 M13）
- 空状态（无视频）：显"导入你的第一个视频"引导

### 决策60 删除视频
- 删整段视频不算违反"永不删除内容"（原则防编辑意外丢内容；删视频是显式主动动作）
- 仅硬删，不做软删/回收站（含 v2）
- 强确认弹窗："将永久删除：视频 + N 段结构 + M 条笔记，不可恢复"
- 级联：视频 + 段落树 + sentences + notes 全删（v1 无派生引用，干净）
- 本地视频文件：保留（只删 DB 记录 + 缩略图，用户自管文件）；在线 URL 视频删 DB + 缓存
- 入口：卡片 ⋯ / 右键 → 删除；v1 单个删，不做多选批量
