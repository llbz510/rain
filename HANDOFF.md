# Rain 项目 — 会话接续文档

> 用途：新会话读取此文件即可接续当前讨论进度。
> 更新时间：2026-07-01（第十二次会话）

## 项目概述

Rain 是一个**个人使用的多模态视频学习工具**，借鉴渐构（modevol.com）的理念，但定位为独立个人工具。核心流程：导入视频 → AI 结构化 → 三区学习界面 + AI 助手问答。

- PRD 索引：`D:\gongju\shengcan\rain\PRD.md`
- 技术栈：Tauri + React + SQLite
- 参考源：https://www.modevol.com/episode/cl3gp8ns7004h01s8179zhpre
- 废案代码（不参考）：`D:\gongju\shengcan\rain\0.1\`

## 第一会话已确认的决策（共 10 项）

### 1. 视频区（原动画区）= 只播原视频
不做手工动画。多模态哲学：内容是结构化转录（纯文本），交互是多模态（AI 助手看画面 + 用户看视频）。

### 2. AI 管线 = 两段式
Stage 1: ASR（三档：字幕>API>本地Whisper）→ Stage 2: LLM 调用做分段+分类+命名+建层+翻译。不清洗文本，不表达 LaTeX/代码。
**调用次数（第四会话更新）：** 短视频单次调用；长视频分块多次调用（由 M18 包装，详见 M18）。原"单次调用"措辞据此更新。

### 3. 段落类型 = 4 种
概念描述、例子、类比、过渡。砍掉了渐构的"自定义"和"标题"（标题改为结构性属性）。

### 4. 层级 = 不限深度，AI 默认 3 层
数据模型支持树形不限深度，AI 默认产出 章节>小节>段落。用户可手动拖拽加深。

> **子项更新索引**（4b/4e 在后续会话引入，此处指向便于定位）：
> - **4b** = reparent 受时间线约束（"自由 reparent"更新为受约束）→ 见决策 42
> - **4e** = 拆分/合并/重排后时间范围自动重算 → 见决策 44（拆分）、决策 45（合并）

### 5. 文本 = 原始转录，不清洗
不清洗口癖/语法，不渲染 LaTeX/代码。逐句时间戳保留，双击句子跳转视频。

### 6. AI 助手 vision = 当前帧 + 手动定帧
管线不做画面理解，AI 助手按需做。默认附带当前播放帧，用户可手动拖到指定帧再提问。

### 7. 三种模型分别可选
ASR 模型（选具体模型）/ 结构化 LLM（纯文本）/ 助手 LLM（必须 vision-capable）。API 档支持国内供应商 + 自定义供应商。

### 8. 视频来源 = 本地 + 在线 URL
支持 YouTube/B站 URL 导入（yt-dlp 解析）。统计封面删除，用原视频缩略图。

### 9. 摘注与随记 = 合并为一个模块
摘注=一键捕获（段落标题行按钮）。随记=右侧面板（AI助手旁），接收捕获段落 + 支持自由笔记。派生=低优先级，v1 预留字段。

### 10. AI 助手快捷操作 = 按段落类型差异化
- 概念描述：变简约/变详细/生成例子/生成类比/分析输入输出/生成概念习题/生成关联习题
- 例子：提取概念/换个例子/变式练习
- 类比：解释对应关系/换个类比/指出类比局限
- 过渡：无
- 通用：解释画面（段落级+vision）/ 总结本节（小节级）
- AI 回答可手动选择存入随记
- 跨视频搜索留待 v2
- 不做每日配额

## 第二会话已确认的决策（共 4 项）

### 11. M05 随播模式布局 = 顶部行+底部行（替代原"父域左/子域右"）
- **顶部行**：左半=章节群，右半=当前章节的小节群，`>`分隔。小节不夹在章节之间。
- **底部行**：当前小节的段落。
- 全部横向滚动。选不同章节→右侧小节更新；选不同小节→底部段落更新。
- 填充=进度（■已播/□未播），单击任意标题跳转。
- 详见 `M05-catalog-zone.md` 的"随播模式布局"节。

### 12. M05-1 随播模式动画
- **进度填充动画**：每个节点填充从左到右（像进度条填满）
- **内容切换动画**：选不同章节/小节时，小节列表和段落列表用横向滑动（新内容从右滑入，旧内容左推），~200ms
- **同级切换**：不滑动，只高亮+自动居中

### 13. M05-2 折叠策略
- 不放就滚：章节群、小节群、段落行各自横向滚动，不折叠不缩略
- 自动居中：当前播放节点自动滚到可视区中间
- 边缘渐隐：左右边缘加渐隐渐变提示"还有更多"
- 暂停时取消跟随：播放时自动居中跟随；暂停时取消，用户可自由滚动；恢复播放→重新居中

### 14. M05 展开模式补充
- 展开模式时：视频区**收起**，目录区展开为导图区**接管视频区空间**
- 水平树形导图，根在左向右展开，结构：章节>小节>段落
- （此布局变化细节属于 M16，待讨论）

## 第三会话已确认的决策（共 9 项）

### 15. 永不删除内容（原"永不删除节点"，第四会话更新）
- **永不删除内容**（核心原则）。节点可删，但删前内容必须迁移。
- 删节点 = 内容自动并到上一级兄弟：段落删→句子并上一段；小节删→其下段落并上一小节；章节删→其下小节并上一章节。
- **章节删除加确认**（显示"X 个小节将并入上一章节"）；段落/小节自动。
- **第一个节点（无上一级兄弟）不可删**；恢复方式 = 再拆。
- 空容器（无内容/无子节点）直接删（兼容原例外）。
- 结构编辑：拆分、reparent（拖拽重排）、重命名、改段落类型、**删除（内容迁移）**。
- "提取为新段落"算移动（内容搬迁、总量不减）。
- 笔记引用句子（sentenceIds），删段落时内容迁移、引用仍指向句子，无级联问题。

### 16. 摘注 = 动作（创建可编辑笔记），非独立实体
- 摘注按钮/按 ` 键 → 在随记面板创建一条**可编辑笔记**（Note）
- 笔记自动关联被摘注段落的全部句子（可跳回），content 初始为空，用户可立即编辑
- 笔记可**继续添加其他段落的句子引用**（多引用、可跨段）
- 笔记引用句子（sentenceIds），拆分/合并/重排/删除（内容迁移）后引用仍指向对应句子，永远有效
- 随记面板仍在（右侧），派生（M11）仍 v2 只预留字段

### 17. Excerpt 实体合并入 Note
- 删除独立 Excerpt 实体，统一为 Note 实体，用 `source` 区分来源
- `source: 'excerpt' | 'user' | 'ai'`（摘注创建 / 手写 / AI回答存入）
- `paragraphId`（单引用）→ `sentenceIds: string[]`（句子级多引用数组；第四会话由段落级改为句子级，见决策18）
- 原 `annotation` 字段被 `Note.content` 取代
- `Paragraph.excerpts` 删除，统一为 `Paragraph.notes`
- M11 `DerivationNode.excerptId` → `noteId`（v2 再改）

### 18. 不存文本快照 + 句子级引用（第四会话更新）
- 笔记引用**句子**（`sentenceIds`），不引用段落。句子永不删，跨合并/拆分/重排/删除（内容迁移）全有效。
- 点击引用跳回看对应句子的当前文本。
- 不额外存储文本快照。

### 19. M16 三种模式布局
- **随播模式**（默认）：左侧目录树 + 顶部目录横条 + 视频(ASR字幕) + 文本区(滚动转录稿) + 右侧Tab面板
- **文本展开模式**：视频收成底部控制栏，文本区展开为全文稿接管视频区位置。左侧目录树和顶部横条仍在
- **目录展开模式**：视频收成底部控制栏，目录横条消失，导图区(水平树形)接管视频区位置。底部有文本预览。左侧目录树仍在（目录展开只占视频区域）

### 20. M16 目录区 = 两个组件（第四会话更新）
- **左侧目录树**：竖状**章节/小节/段落**完整三级导航（原"不含段落"，已改），树形可展开/折叠，三种模式都在。兼作结构编辑面 + 双击跳转面。
- **顶部目录横条**（M05原设计）：章节群>小节群/段落，进度填充，随播和文本展开模式在，目录展开模式消失。与左侧目录树可重复（不强制分工）。

### 21. M16 模式切换
- 视频底部控制栏两个按钮：`文本展开` `导图展开`
- 展开时按钮变 `文本收起` / `导图收起`
- 同一时间只开一个，点另一个自动收起当前的

### 22. M16 右侧面板 = Tab 切换
- AI助手 / 随记，Tab切换，同一时间只显示一个

### 23. M16 尺寸比例 + 视频字幕
- 用户可拖拽分隔线调整，记忆比例
- 默认：左侧目录树:中间:右侧 ≈ 1:7:3；随播模式视频:文本 = 6:4；顶部横条固定约80px
- 视频字幕 = ASR逐句字幕（CC样式叠加在视频上），与文本区滚动转录稿分开

## 第四会话已确认的决策（共 14 项，M18 长视频 + 牵连改动）

### 24. M18 = Stage 2 包装层（决策2 措辞更新）
长视频：转录→[M18：分块→每块 Stage 2→合并]→JSON。Stage 2 每块契约不变。决策2 改为「单次调用（短）/ 分块多次调用（长，由 M18 包装）」。详见 M18。

### 25. M18 触发 = token-based 33%
ASR 完成后、Stage 2 前，数 token。转录文本 > 结构化 LLM 窗口×33% → 分块。安全系数可调（高级设置，默认 33%）。

### 26. M18 分块 = token 预算 + 句子边界
每块~25% 窗口、只在句子边界切、不额外调 LLM、不做语义优化（v1）。否决按字幕段落/按语义。

### 27. M18 上下文传递 = 前情摘要
每块 Stage 2 输入 = 前置块标题 + 上块末段 + 本块句子流。代码抽取、零额外调用、不重叠。

### 28. M18 合并 = LLM 看紧凑元数据
合并 LLM 只看（标题+类型+边界段+术语）、不重读全文，产出统一方案由代码应用到库里完整树。有界、与时长脱钩。能统一结构但不能重做单段语义（残余瑕疵靠决策15 手动改）。

### 29. M18 溢出对策 = 三层
自动重切自愈（失败/截断→对半切重试 N 次）+ 静默截断检测（校验 JSON 完整性）+ 手动兜底（调低阈值重跑失败块）。M19 加结构化 LLM 输出上限约束。

### 30. M18 进度 = 粗 status + 详细 progress
粗 status（M17 列表）+ 详细 progress 对象（导入框：stage/blockCurrent/blockTotal/percent/retrying）。ETA 仅结构化阶段。中断恢复指引归 M15（只原子存 ASR、其余重跑）。

### 31. M18 手动调整分块 = 不做
分块是临时加工件、合并完即消失。用户杠杆：阈值可调 + 树可编辑（决策15）。

### 32. ASR 长音频 = 永不建切块兜底 + M19 准入
本地 Whisper 原生处理长音频（30s VAD 自切、GPU 可接受）；API 用支持长音频的国产供应商。不支持的供应商长视频直接失败。M19 准入：API 须返回句级时间戳。本地 Whisper 句级 = 词级时间戳+按标点分组。M04 加「ASR 输出标准化为 Sentence[]」步骤。

### 33. 翻译 = 原文+翻译并存、新增 translation 字段
仅英文视频有段落级中文翻译（translation 字段）。不替换 text/sentences（保时间戳映射）。补 M04 原 gap。翻译展示待 M07 细化。

### 34. Path B = M04 输出删 text、只存 sentences
LLM 输出不存 text（与 sentences 重复），app 拼接 text 入库（M02 不变）。输出减半、触发线统一 33%、结构上锁死"不清洗"。

### 35. 决策15 更新 = 永不删除内容（详见决策15）
节点可删、内容迁移（段落/小节自动并上一级、章节加确认、首个不可删、再拆恢复）。

### 36. 决策18 更新 = 句子级引用（详见决策18）
笔记引用 sentenceIds，句子永不删，跨所有结构操作有效。

### 37. 决策20 更新 = 左侧目录树完整三级（详见决策20）
左侧树含章节/小节/段落，兼作编辑面+双击跳转面；与顶部横条可重复。

## 第五会话已确认的决策（共 9 项，结构编辑与导航交互 cluster）

### 38. 左树单击/双击/右键语义
- **单击=仅选中**（高亮，不 seek 视频，文本区不滚动、保持跟随视频）。选中是结构编辑前提
- **双击=三区跳转**（见决策40）
- **右键=上下文菜单**（重命名/改类型/删除/提取为新小节/合并，按级别过滤）
- 顶部横条仍单击=跳转（横条纯导航不编辑）。两组件语义不同合理
- M05 原"双击节点重命名"作废，重命名改右键

### 39. 结构编辑执行位置
- **左树**：节点级（重命名/改类型/删除）+ 多节点操作（提取为新小节/合并/reparent）
- **文本区**：单节点三件套（重命名/改类型/删除，via 段落头 ⋯ 菜单）+ 句级拆分
- **顶部横条**：纯导航，不编辑
- **导图区 reparent**：延后单独讨论（非核心，牵动三区同步，归入实时更新议题）
- 多选机制：Ctrl+点选增减、Shift+点选选范围（合并 & 提取新小节共用）

### 40. N3 三区跳转细节
- 双击左树节点：视频 seek 到起点时间（容器=子孙最早句子 start）；文本滚到该节点下最早叶子内容（章节→首小节开头/小节→首段落/段落→自身）；目录横条切上下文+左树选中高亮展开父级滚入可视区
- **播放状态保持**（原在播继续播/原暂停停新点），不强制
- **跨模式照跳**：目录展开模式（视频收控制栏）下双击仍跳（控制栏更新+文本预览滚+导图选中），不打断模式

### 41. N1 实时更新（结构编辑后三区同步）
- **单源真相**：store 唯一真相，所有 UI 订阅，编辑改 store→各组件自动重渲染，无手动同步
- **视频不被结构编辑打断**：句子时间戳不变→视频继续播
- **高亮绑句子不绑段落**（契合决策18 句子级引用）：结构怎么变，当前播放高亮跟着句子走
- 删除当前节点→高亮跟内容并入兄弟；reparent 当前节点→高亮跟句子走；拆分/合并→越过边界自然切

### 42. 结构=时间线 不变量 + reparent 约束（决策4b 更新）
- **铁律**：结构永远等于时间线（段落树是时间线的分层索引）。此不变量使 M07"按结构组织"与"跟随视频"永远同义，无歧义
- 唯一能破坏它的是跨时间区域自由 reparent → **reparent 受时间线约束**：只允许时间线一致移动（如修 AI 误分组）；**跨时间区域移动禁止**
- 非法拖拽：拒绝放置 + 提示"该位置与时间线不符，无法移动；跨时间关联请用笔记引用"
- 分工：reparent=物理时间线结构修正（守时间线）；笔记引用=跨时间认知关联（不受时间线约束）
- 决策4b"自由 reparent"据此更新为受约束

### 43. 文本区段落编辑 UI（变体 A）
- **变体 A 锁定**（原型验证）：类型胶囊常驻 + 悬停浮现 ⋯ 菜单
- 段落头：`[类型 ▾]` 胶囊（单击改类型）+ 标题 + `摘注` 按钮 + `⋯` 菜单（悬停浮现）
- `摘注` 按钮文字为"摘注"（非 `` ` ``），`` ` `` 仅快捷键
- 重命名：菜单→标题原地输入框，回车/Esc/失焦
- 章节/小节标题头同有 ⋯（仅重命名/删除）

### 44. 拆分（提取为新段落）交互
- 文本区**单段落内**拖选连续句子（跨段边界自动截断）
- 选中浮现工具栏：`✂ 提取为新段落 | 全选段落 | 复制`
- 提取→新建兄弟段落（类型默认同原），标题输入框自动聚焦可留空，原段留剩余，时间范围重算（决策4e）
- `+ 摘注选中句` 暂不入工具栏（留 M08 讨论，会改"摘注=整段"语义）

### 45. 合并定义（独立操作，补删除迁移反向缺口）
- **选区约束**：同级 + 同父 + 相邻 + 不混级（选小节合并不能带段落，章节同理；跨父同级也不允许）
- **存活者**：合并后浮现气泡"保留哪个？[A标题(类型)] [B标题(类型)]"→点选保留该节点标题+类型，另一个标题/类型丢弃、句子/子节点按时间序吸收
- **绝不引起时间冲突**：不变量下相邻同级兄弟时间连续，合并=并集相邻范围，天然无冲突
- 段落合并拼句子；小节/章节合并子节点全并入、层级不变；时间范围重算（决策4e）

### 46. 首个节点不可删边界 + 删除确认 UX
- **首个节点**=每父级下第一子节点（无前序兄弟）且非空 → delete **置灰禁用** + hover 提示"首个节点不可删除；如需去除请用合并，或清空后删空容器"
- **合并可绕过保护**（合并双向显式选择，安全）；"首个不可删"只约束 delete
- **空容器直接删**（含空的首个容器，无内容可迁不违反原则）
- **孤节点往上走一级**：某节只一段→合并该节（与相邻小节合并）；某章只一节一段→合并该章。唯一去不掉：整视频一章一节一段（=全部内容）
- 删除确认：段落/小节自动迁移无确认；章节弹确认"X 个小节将并入上一章节"；空容器直删无确认
- 恢复方式=再拆

## 第六次会话已确认的决策（共 7 项，导图区 + M14快捷键）

### 47. 导图区不做 reparent
目录展开模式的导图区 = 纯 view+navigate+select，**不做任何结构编辑**（reparent 全走左树）。理由：决策42 时间线约束下 reparent 收得窄、左树够用；保导图干净；决策39 已标非核心、省实现成本。

### 48. 导图区交互沿用决策38
- **单击节点 = 仅选中**（决策38 扩展到导图）：高亮，不 seek 视频；选中全局共享（决策41），左树同步高亮（**不展开父级、不滚动**，轻量；要全同步用双击）
- **双击节点 = 三区跳转**（N3 扩展到导图）：seek 视频到节点起点 + 预览滚到该节点 + 左树展开父级滚入；目录展开模式下=控制栏时间更新 + 预览滚 + 导图高亮
- 横条仍单击=跳转（纯导航）。三组件语义：左树/导图=单击选+双击跳，横条=单击跳

### 49. 文本预览跟选中（不跟播放）
- 预览显示选中节点的**最早叶子段落**文本（容器→首叶子；段落→自身，与 N3 文本区滚动逻辑一致）
- **粘性跟选中、不跟播放**（否则单击选 A 却显正在播的 C 没意义）
- 进目录展开模式时，选中初始化=当前播放节点（一进来预览显正在播的）；之后只有单击才换
- 视频 independently 播（控制栏显进度）；要看播放处用双击跳

### 50. 节点框内拖动 = scrub（目录展开模式独有）
- 框内横向拖动→映射该节点 [start,end] 时段，拖到哪 seek 到哪；松开播放状态不变
- 离开本节点框=取消；跨节点拖=忽略（已拒 reparent）
- 段落框=细粒度（几句），章节/小节框=粗粒度（整段范围），都可 scrub
- 手势区分：mousedown→不动松开=单击选中；动超阈值=scrub；双击不涉拖动不冲突
- 仅目录展开模式（随播/文本展开用横条 M05 拖拽跳时刻 + 控制栏 scrub）

### 51. 导图区其他细则
- **无右键菜单**（无编辑→无编辑菜单）
- **不支持多选**（多选是编辑用，编辑在左树；导图单选）
- **折叠子树**：节点上展开/折叠箭头点按切换（M05"可手动折叠"）

### 52. 左树编辑→导图自动同步
- 决策41 单源真相：导图订阅 store，左树任何编辑（reparent/重命名/改类型/删除/合并/拆分）→导图自动重渲染
- 删除当前选中节点→高亮跟内容并入兄弟（决策41），导图同步
- scrub 不改选中、只改播放位置；播放高亮绑句子（决策41）随 scrub 移动

### 53. M14 快捷键完整集
- **总则**：非输入态=全局快捷键生效；输入态（AI/随记输入框）=全局禁用、`Enter`发送/`Alt+Enter`换行；树/导图导航=鼠标（无键盘导航）；AI操作=鼠标点按（无快捷键）
- **全局键**：`1`/`2`/`3`=模式切换（随播/文本展开/目录展开）；`` ` ``=摘注播放段（暂停处，指定段用按钮）；`Space`=播停；`←`/`→`=±5s、`Shift+←`/`→`=±10s；`↑`/`↓`=音量；`N`/`P`=下一段落/上一段落起点（结构化seek+选中）；`Del`/`Backspace`=删选中节点（仅左树选中，见下）；`Tab`=切AI↔随记面板
- **`Del` 选中来源约束**（与决策47/48交叉）：选中视觉全局共享（48不变），但 `Del` 仅 `selectionOrigin==='tree'` 触发；store 加 `selectionOrigin:'tree'|'diagram'`；导图选中的节点按Del无反应（保决策47导图纯看）。删除两路径（Del键+右键菜单）都左树作用域
- **与渐构差异**：`1/2/3`直接对应三模式（非渐构原映射）；新增 N/P、Shift箭头、Tab、Del；不设树键盘导航、不设AI操作快捷键
- 详见 M14-keyboard-shortcuts.md

## 第七次会话已确认的决策（共 9 项，M17 完整 + 观看进度数据模型）

### 54. M17 导航模型 = 独立主页 / 单视频 / 多标签栏 v2
- App 打开→视频列表（满屏主页）；点视频→进入学习界面（替换列表）；学习界面"返回"→回列表。**同一时间只开一个视频**。
- 多标签栏（多视频同时开）随 M11 进 v2，不在 v1 做。理由：M16 三模式均为单视频四区；多标签栏 M11 明定 v2。

### 55. M17 导入生命周期 + 状态粒度
- 导入一开始即建 Video 记录（status=pending），**立即作为卡片出现在列表**；卡片显粗状态 + 进度百分比，live 更新。
- M30"导入框"= 详细进度（stage/blockCurrent/blockTotal/percent/retrying），可关闭/最小化；视频**后台继续处理**，列表卡 live 更新，完成转 ready。失败→卡片显 failed + 重试入口。
- **状态粒度分工**：卡片=粗状态+百分比（轻）；导入框=M18 详细 progress（重）。同源 store。
- **并发导入**：v1 一次只导一个，其余排队。列表显示所有状态视频（pending/processing/ready/failed）。

### 56. 观看进度数据模型 + M05 ■/□ 语义澄清（跨 M02/M05/M17）
- **M05 目录■/□ = 瞬时当前播放位置指示**（非持久）：当前播放时间之前=■、所在节点=当前、之后=□；拖动播放头回退→重算，不跨会话记忆。澄清决策 11/12 原含糊措辞。
- **Video 新增 2 字段**（改 M02）：`position`（最远到达秒数，持久单调，用于列表进度条 + 重开自动续播）+ `lastStudiedAt`（最近学习时间，用于"最近学习"排序）。
- **重开视频自动续播到 position**（不弹框，直接跳）。
- **position-based（一个数），不上逐句 played**。列表进度条与 M05 ■/□ 两套数据源、两处显示，靠"自动续播"桥接。

### 57. M17 ready 卡片 + 网格
- 缩略图网格（v1 只网格，不切换列表）；ready 卡显：缩略图/标题/底部细进度条(position/duration)+时长/最近学习小字/语言·来源角标/已看完✓
- 非 ready 卡：缩略图蒙层+状态徽章（决策55）

### 58. M17 排序
- 默认最近学习（lastStudiedAt 降序）；三档可切换：最近学习/导入时间/名称；自定义手动排序不做

### 59. M17 检索：无筛选 + 标题搜索
- 不做筛选/facet/标签/文件夹；搜索=关键词搜标题；转录稿搜索 v2；无状态筛选→失败/处理中与 ready 混排靠徽章区分

### 60. M17 删除视频
- 删整段视频不违反"永不删除内容"；仅硬删（含 v2 无软删）；强确认（视频+N段+M笔记不可恢复）；级联全删；保留本地视频文件（只删DB+缩略图）；卡片菜单单个删，不做多选批量

### 61. M17 导出 — 暂不做
- v1 不导出（学习数据/视频文件均不导），搁置

### 62. M17 收尾交互
- 点非 ready 卡→重开导入框；顶栏=排序选择器+搜索框+导入按钮；空状态显"导入你的第一个视频"

## 当前讨论进度

### 已完成的待讨论项
| 编号 | 内容 | 结果 |
|------|------|------|
| M05-1 | 随播模式动画 | ✅ 已确认（见决策12） |
| M05-2 | 折叠策略 | ✅ 已确认（见决策13） |
| M05-3 | 删除级联规则 | ✅ 作废（永不删除内容，见决策15） |
| M02 更新 | Excerpt 合并入 Note | ✅ 已确认（见决策17） |
| M08 更新 | 摘注=创建可编辑笔记 | ✅ 已确认（见决策16） |
| M16 | 界面布局 | ✅ 已确认（见决策19-23） |
| M18 | 长视频分段处理 | ✅ 已确认（见决策24-34，第四会话） |
| 结构编辑与导航交互 cluster | N1/N2/N3 + 单击双击语义 + 首个节点边界 + 合并定义 + 结构=时间线不变量 | ✅ 已确认（见决策38-46，第五会话） |
| 导图区 reparent + 跨组件一致性 | 导图不做reparent/沿用决策38/预览跟选中/节点框scrub/无右键不多选/编辑自动同步 | ✅ 已确认（见决策47-52，第六会话） |
| M14 快捷键 | 模式1/2/3+摘注`+视频控制+N/P跳段+Del删节点(仅左树)+Tab切面板；树/AI无键盘快捷键 | ✅ 已确认（见决策53，第六会话） |
| M17 视频列表 | 导航/导入生命周期/进度数据模型+M05■/□澄清/卡片/排序/检索/删除/收尾 | ✅ 已确认（见决策54-62，第七会话） |
| M13 视觉设计 | 排版/令牌/M17列表细节/专项视觉(字幕/导图节点/右侧面板/动效) | ✅ 已确认（见决策73-81，第九会话） |
| 撤销/取消 + M15 数据持久化 | 撤销栈/导入取消/AI停止 + SQLite schema/目录/API Key/中断恢复 | ✅ 已确认（见决策83-84，第十会话） |
| ASR 语言检测 | 三档纯自动检测/不可手动覆盖/en触发翻译 | ✅ 已确认（见决策85，第十会话） |
| M07 翻译展示 + 收尾 gaps | 翻译呈现/不句级高亮/译文开关 + AI上下文取原文 + 撤销栈边界 + 取消重试=中断恢复 + 合并失败处理 + 字幕/译文开关区分 | ✅ 已确认（见决策86-91，第十一次会话） |
| M20 技术架构 | LLM前端直连/SQL前端直连/whisper-rs/yt-dlp自装/convertFileSrc/Tauri event进度/tokio调度/Zustand边界 | ✅ 已确认（见决策92-99，第十二次会话） |

### M05 目录区 = 已确认 ✅
### M16 界面布局 = 已确认 ✅
### M18 长视频分段处理 = 已确认 ✅
### 结构编辑与导航交互 cluster = 已确认 ✅
### M13 视觉设计 = 已确认 ✅
### 撤销/取消 + M15 数据持久化 = 已确认 ✅

## 待讨论的模块（按优先级排序）

### 低优先级（v1 PRD 已全部确认，以下为 v2 或实现前可补细节）
1. **M11 派生系统** — v2 实现

> v1 全部模块 PRD（含 M20 技术架构）已确认完毕。下一步用 `writing-plans` 拆实现计划。

## 文件结构

```
D:\gongju\shengcan\rain\
├── PRD.md                    # 模块索引（已更新至 v4.5）
├── HANDOFF.md                # 本文件
├── M01-positioning.md        # ✅ 产品定位与边界
├── M02-data-model.md         # ✅ 数据模型（第四会话：Sentence.id/Note.sentenceIds/Paragraph.translation）
├── M03-video-import.md       # ✅ 视频输入与导入
├── M04-ai-processing.md      # ✅ AI处理管线（第四会话：Path B删text/translation/ASR标准化/长音频）
├── M05-catalog-zone.md       # ✅ 目录区（第五会话：左树含段落/单击双击语义/三区跳转/结构编辑全套/合并/reparent约束/首个节点边界/结构=时间线不变量；第六会话：导图区交互全套-单击选/双击跳/框内scrub/无reparent无右键/预览跟选中）
├── M06-video-zone.md         # ✅ 视频区
├── M07-text-zone.md          # ✅ 文本区（第五会话：结构=时间线不变量澄清+文本区编辑UI+拆分工具栏）
├── M08-excerpt-notes.md      # ✅ 摘注与随记系统（第四会话：删除规则更新+句子级引用）
├── M10-ai-assistant.md       # ✅ AI助手
├── M11-derivation-system.md  # ⬜ 派生系统（低优先级）
├── M13-visual-design.md      # ✅ 视觉设计（决策63-81，配色/排版/令牌/列表/专项全部定稿）
├── M14-keyboard-shortcuts.md # ✅ 快捷键（第六会话：完整集-模式/摘注/视频/N-P跳段/Del仅左树/Tab；树&AI无键盘快捷键）
├── M15-data-persistence.md   # ✅ 数据持久化（第十会话：完整schema/统一node表/API Key明文/目录结构/中断恢复/撤销栈）
├── M16-layout.md             # ✅ 界面布局（三种模式+四区+Tab面板；第六会话：目录展开模式导图区交互+文本预览跟选中细化）
├── M17-video-list.md         # ✅ 视频列表与管理（第七会话：导航/导入生命周期/进度模型+M05■/□澄清/卡片/排序/检索/删除/收尾）
├── M18-long-video.md         # ✅ 长视频分段处理（第四会话确认）
├── M19-settings.md           # ✅ 设置与模型管理（第四会话加模型准入约束；第九会话模型导入选择决策82；第十会话API Key明文存SQLite）
├── M20-architecture.md       # ⬜ 技术架构
└── 0.1\                      # 废案代码（不参考）
```

`✅ = 已确认`  `🔄 = 部分确认`  `⬜ = 待讨论`

## 建议技能（下次会话）

讨论 PRD 时建议加载以下技能：

| 技能 | 用途 |
|------|------|
| `zoom-out` | 画系统全貌地图，帮助理解模块关系 |
| `grill-with-docs` | 逐个决策点拷问+给推荐方案+定了我即写进文档 |
| `prototype` | UI相关决策（M16布局/M13视觉）做可点击HTML原型让用户选 |
| `image-analysis` | 用户发图片时用千问视觉模型读取（config已配好API Key） |

后续阶段：
| 技能 | 用途 |
|------|------|
| `writing-plans` | PRD全部确认后，拆成有顺序的实现计划 |
| `to-issues` | 把实现计划拆成可独立认领的issue |
| `handoff` / `session-save` | 保存进度 |

## 新会话接续方式

新会话中发送以下内容即可接续：

```
读取 D:\gongju\shengcan\rain\HANDOFF.md，然后继续讨论待讨论的模块。请用上所有合适的skill和我更细致的讨论，在讨论过程中有什么和之前决策冲突或者让你不确定的地方要问我
```

接续后第一件事：PRD 决策到 85。M15 已补完整（决策84），ASR 语言检测已定（决策85）。下一步二选一——(a) 用 `writing-plans` 把 PRD 拆成有顺序的实现计划；(b) 先补 M20 技术架构（前后端分工细节）再拆计划。

> 本会话（第五次）原型的可点击 HTML 在 `prototype\text-edit-prototype.html`，已锁定变体 A，可删可留作参考。
> 第六次会话无原型，纯决策讨论（导图区 cluster + M14 快捷键）。
> 第七次会话无原型，纯决策讨论（M17 视频列表完整 + 观看进度数据模型，跨 M02/M05/M17）。
> 第八次会话（2026-06-27）：M13 配色定稿（决策63-72）+ M16 控制栏位置修订（v4.5）；原型 prototype\m13-color-mockup.html v2 验证通过；排版已提推荐方案待下次确认。


## 第八次会话已确认的决策（2026-06-27）

**M13 视觉设计 — 配色定稿（决策63-72）**：暗色单主题；中性色阶借渐构四档；无品牌强调色、控件纯灰白；段落四类为全界面唯一彩色（概念=蓝/例子=绿/类比=橙/过渡=灰）；选中=变暗、播放=变亮、同现=暗底叠白亮条；■进度=类型色（段落）/中性（容器），节点文字白；文本区类型胶囊保留彩字；列表卡片进度条中性+已看完✓、非就绪蒙层+状态徽章（失败红/处理中黄/排队灰）。配色经原型 prototype\m13-color-mockup.html（v2，两屏可切）验证通过。

**M16 界面布局 — 控制栏位置修订（v4.5）**：随播模式控制栏收进视频区内；控制栏始终压在文本区正上方；目录展开控制栏位置不变（导图区/控制栏/文本预览）、文本展开控制栏上滑到目录横条下方；左树/右面板全高不被截断；控制栏内容=进度条/暂停继续/音量/设置(倍速·清晰度·字幕开关)/文本展开/导图展开/全屏；返回列表在顶栏左上。

**进行中/待续**：排版（字体族/字号阶梯/行距）已提推荐方案待确认；令牌（间距/圆角/阴影）；M17 列表视觉细节（卡片尺寸/网格/信息条/角标/空状态）；专项视觉（字幕样式/导图节点/右侧面板/动效）。

## 第九次会话已确认的决策（2026-06-28）

**M13 视觉设计 — 全部定稿（决策73-81）**：排版（字体族=系统兜底无衬线单族+等宽数字特性，决策73；字号5档18/16/14/13/12+字重400/600/700，决策74；行距正文1.7/标题1.3/次正文1.5，决策75）；令牌（间距4基准阶梯4/8/12/16/20/24/32/48、圆角5档0/4/8/12/胶囊、阴影仅浮层一档、顶栏/控制栏40/横条80，决策76）；M17列表细节（响应式网格minmax240、缩略图16:9、语言·来源右下角标、已看完=信息条文字、空状态，决策77）；专项视觉（字幕=底框+400+阴影，决策78；导图节点=类型色填充+SVG圆角弯折连线+滚轮缩放0.4-2.5/空白拖拽，决策79；右侧面板AI助手对话=气泡/当前帧/快捷芯片/输入框Enter+Alt+Enter，决策80；动效=时长三档120/200/320+ease-out/进度实时跟播/尊重减少动效，决策81）。原型 `prototype\m13-typo-mockup.html`（三屏：学习界面/视频列表/导图模式）验证通过。

**M19 模型导入与选择补全（决策82）**：模型池 + 角色选择模式；添加模型（API类=供应商预置+自定义/baseURL/Key/模型名/别名；本地Whisper=单条目+大小可切+下载；字幕档无需模型）；角色选择（ASR三档/结构化LLM/助手LLM只列vision）；测试按钮 + vision手动勾选。原型 `prototype\m19-settings-mockup.html`。

**v1 PRD 全部模块确认完毕**，下一步用 `writing-plans` 拆实现计划。

> 第八次会话（2026-06-27）：M13 配色定稿（决策63-72）+ M16 控制栏位置修订（v4.5）。
> 第九次会话（2026-06-28）：M13 排版/令牌/M17列表细节/专项视觉全部定稿（决策73-81）；导图区原型重写为递归树+SVG圆角连线+缩放拖拽；AI助手对话面板原型；动效令牌统一；M19 模型导入与选择补全（决策82）。v1 PRD 收口。
> 第十次会话（2026-06-30）：撤销/取消（决策83）+ M15 完整 schema/目录/API Key 明文/中断恢复/统一 node 表（决策84）+ ASR 语言纯自动检测（决策85）。
> 第十一次会话（2026-07-01）：翻译展示（决策86）+ AI 上下文取原文（决策87）+ 撤销栈边界（决策88）+ 取消重试=中断恢复（决策89）+ 合并失败处理（决策90）+ 字幕/译文开关区分（决策91）。M07 唯一未决项收口。
> 第十二次会话（2026-07-01）：M20 技术架构定稿——LLM 前端直连/SQL 前端直连/whisper-rs/yt-dlp 自装/convertFileSrc/Tauri event 进度/tokio 调度/Zustand 边界（决策92-99）。M20 从低优先级待讨论转已确认。

## 第十次会话已确认的决策（2026-06-30）

### 决策83 撤销/取消
- **导入取消**：导入进度框加"取消"按钮→中止 ASR/Stage2→视频标记 `cancelled`（列表显"已取消"，可重试或删除）。半成品结构化丢弃。
- **AI 助手取消**：生成中显"停止"按钮→中断流式输出，已收到部分保留在对话里。
- **撤销**：结构编辑轻量撤销栈，`Ctrl+Z`，会话内（不跨会话持久），最近 ~20 步。每个操作记逆操作（删=迁回／合并=拆开／拆=合并／reparent=移回／重命名=原名／改类型=原类型）。`Ctrl+Y` 重做可选。跨会话不保留；深层恢复仍靠决策15"再拆"。

### 决策84 M15 数据持久化
- **目录结构**：`Rain/` 下 `rain.db`/`config.json`/`videos/<videoId>/`/`thumbnails/<videoId>.jpg`/`whisper-models/`
- **node 统一表**：章/节/段统一一张 `node` 表，`kind` 区分，`parent_id` 自引用成树（支持不限深度，决策4）
- **API Key 不加密**：明文存 SQLite `setting` 表（`key=api_key.<模型别名>`，value=明文）
- **本地视频引用原路径**：不复制；原文件移走→列表标"文件缺失"
- **中断恢复**：`video.stage` 标记阶段（asr/stage2/merging）；ASR 原子持久化；重开：有 ASR→跳过重跑 Stage2，无→重跑 ASR，Stage2 块中断→重跑该块（M18）
- **补字段**：`status` 加 `cancelled`（决策83）、加 `error_message`、`note.sort_order`/`node.sort_order`
- 详见 M15-data-persistence.md（v5.0）

### 决策85 ASR 语言检测 = 纯自动
- 三档 ASR 纯自动检测语言，不需用户选、不可手动覆盖
- 字幕：字幕轨语言标签/内容判断；API：供应商须支持自动检测（M19 准入约束）；本地 Whisper：内置语言检测
- ASR 跑完自动填 `Video.language`；`en`→触发翻译（决策33），`zh`/`other`→不翻译
- 多语言混杂：v1 不做特殊处理，按主语言走

## 第十一次会话已确认的决策（2026-07-01）

### 决策86 翻译展示（补 M07 唯一未决项）
- **呈现**：每个英文段落块下方紧贴一块中文翻译（同段落内视觉成组）；随播模式与展开模式同样呈现
- **不句级高亮**：随播模式英文逐句高亮跟着播放走，中文翻译整段显示、不做句级同步高亮（段落级粒度）
- **控制栏「译文」开关**：挨着字幕开关；仅英文视频（有 translation 字段）显示，非英文视频隐藏；**默认开**，可一键关
- **字幕区不显翻译**：视频画面 ASR 字幕只显英文原文逐句（M13 决策78），翻译只在文本区
- 详见 M07-text-zone.md

### 决策87 AI 助手上下文取原文（补 M10 gap）
- 英文视频时，段落级/小节级/自由对话的 AI 上下文取 `sentences` 原文，**不含 `translation`**
- 翻译服务人类阅读、不喂 AI（翻译有损、AI 为 vision 强模型能直接处理英文、省 token）
- 边缘场景（用户问"这翻译对不对"）由用户把翻译贴进自由对话

### 决策88 撤销栈边界（补决策83 措辞）
- `Ctrl+Z` 结构撤销栈**只管结构编辑**（重命名/改类型/删/合并/拆/reparent）
- 随记面板编辑笔记内容、AI 对话输入框 → 靠**各自输入框原生 undo**（textarea 自带，会话内有效），不进结构栈
- 两套独立、不互通；均不跨会话

### 决策89 导入取消后重试 = 同中断恢复一套逻辑
- cancelled 状态视频点"重试" → 走 M15 中断恢复同一套逻辑：看 `video.stage`，有 ASR 跳过重跑 Stage2、无则重跑 ASR、Stage2 块中断重跑该块；半成品结构化丢弃（决策83）
- 区别只在入口：列表卡"重试"按钮 vs 重开视频自动恢复

### 决策90 M18 合并阶段 LLM 失败处理（补决策29 缺口）
- 合并 LLM 失败/截断 → 不影响已入库的各块结构（块本身 Stage2 已完成并持久化）
- 视频标 failed + 提示"结构合并失败，可重试"：重试只重跑合并那一次（不重跑各块）
- 可"跳过合并"以分块结果直接用（各块独立树、未统一层级），进学习界面后靠决策15 手动改

### 决策91 字幕开关 vs 译文开关（补 M16 控制栏）
- 控制栏两个独立开关：「字幕」控制视频区 ASR 字幕显隐（所有视频显示）、「译文」控制文本区翻译块显隐（仅英文视频显示，决策86）
- 两者独立、互不影响；文案用"字幕""译文"区分

## 第十二次会话已确认的决策（2026-07-01，M20 技术架构）

### 决策92 LLM 调用 = 全部前端直连
- Stage2/合并/AI 助手流式对话的 LLM 调用都在 React 侧（OpenAI 兼容 SDK）
- 前端从 setting 表读 API Key（决策84 明文），直连供应商
- AI 助手流式：SSE 直达 UI，AbortController 原生取消（决策83）
- Rust 后端不代理 LLM，专注 ASR/yt-dlp/文件 I/O

### 决策93 SQLite 访问 = 前端直连 SQL（tauri-plugin-sql）
- tauri-plugin-sql，前端 Zustand action 里直接 exec SQL
- 复杂事务用 BEGIN/COMMIT 包一层保原子
- 不在 Rust 侧封装 DAL/IPC，省一层 binding

### 决策94 本地 Whisper = whisper-rs Rust binding
- whisper-rs crate（whisper.cpp 的 Rust binding），纯 Rust 编译、无外部运行时
- 模型存 whisper-models/（决策84），词级时间戳+标点分组为句级（决策32）

### 决策95 yt-dlp = 用户自装加 PATH
- 应用不打包 yt-dlp，用户自行安装加 PATH
- 在线 URL 导入前检测可调用，缺失则提示安装+给官方链接
- Rust 子进程调用 yt-dlp

### 决策96 视频播放 = convertFileSrc + assetProtocol scope 全放行
- convertFileSrc(path) 转 asset:// 给 video src
- Tauri 配置 assetProtocol.scope: ["**"] 全放行（个人不联网省白名单）
- 本地引用原路径、在线下载后也是本地文件，统一走这套；缩略图同理

### 决策97 进度推送 = Tauri event
- Rust emit("progress", payload) 推进度，前端 listen 监听
- payload = 决策30 progress 对象；完成/失败/取消也走 event，不轮询

### 决策98 长任务调度 = tokio async task
- ASR/合并等长任务 tokio async task 跑，不阻塞主线程
- invoke 启动任务后立即返回，进度走 event
- 并发=1（决策55），前端维护队列；取消用 CancellationToken（决策83）

### 决策99 Zustand 状态边界 = UI 会话态 + 当前视频缓存
- UI 会话态：当前视频 id/选中节点/selectionOrigin/播放位置/三模式/撤销栈/字幕译文开关/AI 面板/随记编辑态/导入队列
- 当前视频缓存：node 树+sentences+notes，查库一次缓存，编辑改缓存+写库
- 不缓存其他视频、不缓存列表（每次查库）；业务数据一律 SQLite，重开从库重读
- 详见 M20-architecture.md（v5.2）
