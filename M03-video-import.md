# M03 — 视频输入与导入

## 支持的来源

| 来源 | 说明 |
|------|------|
| 本地文件 | 用户选择本地视频文件导入（mp4/mkv/webm 等） |
| 在线URL | 粘贴 YouTube / B站 URL，自动解析视频流 + 字幕 |

## 导入流程

```
用户输入（文件选择 or URL粘贴）
  → [URL解析] yt-dlp 获取视频信息 + 字幕（若在线）
  → [ASR] 三档选择（字幕>API>本地Whisper）
  → [LLM结构化] 生成段落树
  → [完成] 进入学习界面
```

## URL 解析

- 使用 yt-dlp 或类似方案（Tauri Rust 后端调用）
- 获取：视频流（用于播放）、字幕（若平台有自动字幕）、视频元信息（标题、缩略图、时长）
- YouTube：通常有自动字幕，直接作为 ASR 字幕档
- B站：部分视频有 CC 字幕，无字幕的视频走 API/Whisper

## 视频缩略图

- 使用原视频缩略图（从 YouTube/B站 元信息获取，或本地视频截取首帧）
- **不做统计封面**（M12 已删除）

## 长视频

- 超长视频（如 2 小时课程）的分段处理见 M18
- 导入时检测时长，超过阈值提示用户（不阻断，仅提示）
