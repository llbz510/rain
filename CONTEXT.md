# Rain 领域语言

> 状态：Active
> 更新日期：2026-07-26
> 作用：让用户、开发者和 AI 用同一组词讨论 Rain，避免同一个词在 PRD、代码和测试里表达不同含义。

## 模型配置（Model configuration）

一组已保存的供应商、接口地址、模型名称、凭据、模型类型和声明能力。接口地址、模型名称或凭据变化后，应视为实质不同的配置。

## 角色（Role）

模型配置承担的一项职责。Rain 当前有三个角色：ASR、结构化、助手。

## 能力检查（Capability check）

用可执行检查判断一份模型配置当前能否承担一个角色。连接成功不等于能完成结构化；文本助手检查通过也不等于支持图像。

## Compatible

当前配置通过了指定角色的能力检查。这个结论属于“配置 + 角色”组合，不属于某个供应商或模型名称本身。

## Verified

除了能力检查通过，还存在一份可定位的 Rain 完整真实 E2E 证据。普通健康检查不能产生 `Verified`。

## Unavailable

检查失败、尚未检查，或者配置变化导致旧结果过期，因此当前不能有把握地把该角色交给这份配置。

## 能力指纹（Capability fingerprint）

能力记录中用于识别“被检查配置 + 角色”的非明文标识。模型名称、接口地址或 Key 变化时，旧指纹不再匹配，旧结果必须失效。
