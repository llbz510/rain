# M05 — 目录区

> 状态：已确认（v4.3 导图区交互 cluster 确认完毕）
> 借鉴渐构设计，具体交互细节已定。

## 目录区的两个组件

目录区由**两个独立组件**组成，在布局中位置不同（详见 M16）：

| 组件 | 位置 | 显示内容 | 作用 |
|------|------|---------|------|
| 左侧目录树 | 界面最左侧竖栏 | **章节/小节/段落完整三级**，树形可展开/折叠 | 全局导航 + 结构编辑面 + 双击跳转面 |
| 顶部目录横条 | 视频区上方横条 | 章节群>小节群（顶行）+ 当前小节的段落（底行），带进度填充 | 进度跟踪，快速切换同级节点（纯导航，不编辑） |

两种组件并存，可重复：左侧树=找位置+编辑+跳转，顶部横条=看进度+快速切换。

### 三种模式下的目录区表现

| 模式 | 左侧目录树 | 顶部目录横条 | 额外 |
|------|-----------|------------|------|
| 随播模式 | ✅ 在 | ✅ 在（章节>小节/段落，进度填充） | — |
| 文本展开模式 | ✅ 在 | ✅ 在 | — |
| 目录展开模式 | ✅ 在 | ❌ 横条消失，变为水平树形导图接管视频区 | 导图+底部文本预览 |

### 信息展示

- 填充：■=已播，□=未播，填充动画从左到右（像进度条填满）
- **■/□ 语义 = 瞬时当前播放位置指示**（非持久记忆）：当前播放时间之前=■、所在节点=当前、之后=□；拖动播放头回退→重算（■ 可撤回），不跨会话记忆。跨会话"看过到哪"由 `Video.position`（最远到达，持久）负责，仅用于列表进度条 + 重开自动续播，**不**显示在本目录■/□。重开时因自动续播到 position，瞬时■/□ 自然显示到 position
- 颜色：区分段落类型（概念描述/例子/类比/过渡）；具体配色与节点文字色见 M13 决策66/70
- 明暗：当前(播放)=变亮、同级=默认、选中=变暗第三态（见 M13 决策67/68）

### 交互

**左侧目录树：**
- **单击节点 = 仅选中**（高亮，不 seek 视频，文本区不滚动，保持跟随视频）。选中是结构编辑的前提
- **双击节点 = 三区跳转**（见下"三区跳转 N3"）
- **右键节点 = 上下文菜单**（重命名/改类型/删除/提取为新小节/合并，按节点级别过滤可用项）
- **拖拽 = reparent**（受时间线约束，见"结构编辑"节）
- **Ctrl+点选 / Shift+点选 = 多选**（用于合并、提取为新小节）

**顶部横条：**
- 单击任意标题（章节/小节/段落）：跳转到对应起点，目录进度随之更新
- 选不同章节：右侧小节列表更新为该章节的小节
- 选不同小节：底部段落列表更新为该小节的段落
- 拖拽：跳转到段落某时刻
- 悬停：进一步查看和交互
- 顶部横条**纯导航，不支持任何编辑**

**导图区（仅目录展开模式）：**
- **单击节点 = 仅选中**（决策38 扩展到导图）：高亮，不 seek 视频；选中全局共享（决策41），左树同步高亮（不展开父级、不滚动——轻量；要全同步用双击）
- **双击节点 = 三区跳转**（N3 扩展到导图）：seek 视频到节点起点 + 预览滚到该节点 + 左树展开父级滚入可视区；目录展开模式下=控制栏时间更新 + 预览滚 + 导图高亮
- **节点框内拖动 = scrub**：框内横向拖动→映射该节点 [start,end] 时段，拖到哪 seek 到哪；松开播放状态不变（原播继续从新位置、原停停在新位置）。离开本节点框=取消。段落框=细粒度（几句），章节/小节框=粗粒度（整段范围），都可 scrub。目录展开模式独有（该模式横条消失，导图接管"拖拽跳时刻"的活）
- **手势区分**：mousedown 节点→不动松开=单击选中；动超阈值=scrub；双击不涉拖动不冲突
- **不做 reparent / 无右键菜单 / 不支持多选**：导图区纯 view+navigate+select，结构编辑全走左树
- **折叠子树**：节点上展开/折叠箭头点按切换（M05"可手动折叠"）
- **文本预览**（底部）：显示选中节点的**最早叶子段落**文本（容器→首叶子；段落→自身，与 N3 文本区滚动逻辑一致）；**粘性跟选中、不跟播放**（否则点A显C没意义）；进模式时选中初始化=当前播放节点（一进来预览显正在播的）
- **左树编辑→导图自动同步**（决策41 store 订阅，导图重渲染）；删除选中节点→高亮跟内容并入兄弟（决策41），导图同步

### 三区跳转（N3）

双击左侧目录树节点，三区同步跳转：

| 区 | 动作 |
|----|------|
| 视频区 | seek 到该节点起点时间（容器=子孙最早句子 start，决策4e） |
| 文本区 | 滚到该节点下最早叶子内容：点**章节**→滚到首小节开头；点**小节**→滚到首段落；点**段落**→滚到自身 |
| 目录区 | 顶部横条切到该节点上下文 + 左树选中高亮、自动展开父级、滚入可视区 |

- **播放状态**：seek 后**保持原状态**（原在播→从新点继续播；原暂停→停在新点），不强制播放/暂停
- **跨模式**：三种模式都生效。目录展开模式（视频收成控制栏）下双击仍跳转：控制栏时间/进度更新 + 文本预览滚到节点 + 导图区选中该节点，不打断当前模式

## 已确认决策（v4.1 及之前会话）

### 展开模式画法（#1）

- **水平树形导图**：根节点（视频）在左，章节/小节/段落向右层层展开。
- 横向滚动容纳深度，纵向滚动容纳同级多节点。
- 节点显示：类型色边框 + 已播填充 + 标题 + 时长。
- 默认全展开，可手动折叠子树。

### 手动加深（#4）

**语义（4a）**：手动加深用于追加**容器层级**（章节/小节），段落恒为叶子节点。用户新划段落归属上一级小节，不增加深度。M02 容器/叶子二分不变。

**操作（4b）**：两者结合——
- 拖拽 reparent：拖到节点上=成为其子级；拖到上方/下方=同级前后移（三段落点，4c）。覆盖通用重排/嵌入。
- 「提取为新小节」：选中若干段落→提取→自动建新小节（当前小节的子级）并装入。批量加深。

**拆分段落（4d）**：「提取为新段落」——在长段落框选**连续**句子→提取→建原小节下兄弟段落→填标题/类型(默认同原)→原段落留剩余，时间重算。与「提取为新小节」对称。新段落是兄弟节点，非原段落子节点。连续选择约束（禁非连续，避免时间空洞段落）。

**时间范围（4e）**：派生不存储。叶子=自己句子 min/max；容器=子孙 min/max。编辑后自动重算。可点击容器跳到首子节点起点。

**约束（4f）**：可拖整棵子树；段落不能成为段落的父节点；无人工深度上限。

**通用编辑**：右键节点重命名（双击=三区跳转，见"交互"节）；段落类型可下拉改（4 种）；移动=拖拽 reparent。

### 随播模式布局（替代原"父域左/子域右"设计）

```
┌──────────────────────────────────────────────────────────┐
│ [章节1] [章节2] [章节3] > [小节2.1] [小节2.2] [小节2.3]   │ ← 顶部行
│  ■已播  □当前  □未播     ■已播   □当前   □未播          │   横向滚动
├──────────────────────────────────────────────────────────┤
│ [段落2.2.1] [段落2.2.2] [段落2.2.3] [段落2.2.4]          │ ← 底部行
│   ■已播      ■已播       □当前       □未播             │   横向滚动
└──────────────────────────────────────────────────────────┘
```

- 顶部行：左半=章节群，右半=当前章节的小节群，`>`分隔。小节不夹在章节之间。
- 底部行：当前小节的段落。
- 选不同章节 → 右侧小节列表更新；选不同小节 → 底部段落列表更新。
- 全部横向滚动，当前节点自动居中，左右边缘渐隐提示"还有更多"。

### 随播模式动画（#2）

- **进度填充动画**：每个节点的填充从左到右（像进度条填满），■=已播 □=未播
- **内容切换动画**：选不同章节/小节时，小节列表和段落列表用**横向滑动**（新内容从右滑入，旧内容左推）。与横向滚动方向一致，不突兀。~200ms。
- **同级切换**（如段落2.2.1→2.2.2）：不滑动，只高亮+自动居中变化。

### 折叠策略（#3）

- **不放就滚**：章节群、小节群、段落行各自横向滚动，不折叠、不缩略
- **自动居中**：当前播放节点自动滚到可视区中间
- **边缘渐隐**：左右边缘加渐隐渐变，提示"还有更多"
- **暂停时取消跟随**：播放时自动居中跟随播放进度；暂停时取消跟随，用户可自由滚动。恢复播放→重新居中到当前节点。
- 章节群、小节群、段落行同理，三套规则一致。

## 结构=时间线 不变量（核心铁律）

**结构永远等于时间线**：段落树是对视频时间线的分层索引。任何编辑都不得破坏此不变量。

- 默认 AI 按时间顺序切分 → 结构=时间线 自然成立
- 所有编辑必须保不变量；唯一能破坏它的是**跨时间区域的自由 reparent**，故 **reparent 受时间线约束**（见下）
- 此不变量使 M07"按段落树结构组织"与"自动滚动跟随视频进度"永远同义，无歧义

## 结构编辑

结构编辑分两处执行：**左树**（节点级 + 多节点操作）、**文本区**（单节点三件套 + 句级拆分）。

| 编辑动作 | 左树 | 文本区 | 触发方式 |
|--------|:---:|:---:|--------|
| 重命名 | ✅ | ✅ | 右键/⋯菜单→标题原地变输入框，回车确认/Esc 取消/失焦确认 |
| 改类型 | ✅ | ✅ | 类型胶囊单击→4种下拉（快捷）；或菜单 |
| 删除 | ✅ | ✅ | 右键/⋯菜单→按删除规则 |
| 拆分（提取为新段落） | ❌ | ✅ | 文本区拖选连续句子→工具栏"提取为新段落"（仅本段内选） |
| 提取为新小节 | ✅ | ❌ | 左树多选段落（Ctrl/Shift）→菜单 |
| 合并 | ✅ | ❌ | 左树多选同级相邻节点→菜单→显式选存活者 |
| 重排 reparent | ✅ | ❌ | 左树拖拽（受时间线约束） |

**多选机制（合并 & 提取新小节共用）**：Ctrl+点选增减单个、Shift+点选选范围；选区高亮，右键任一选中节点菜单对选区生效。

### 拆分（提取为新段落）
- 在文本区**单段落内**拖选连续句子（跨段边界自动截断）
- 选中浮现工具栏：`✂ 提取为新段落 | 全选段落 | 复制`
- 提取→新建兄弟段落（决策4d：类型默认同原），标题输入框自动聚焦（可留空），原段落留剩余句子，时间范围自动重算（决策4e）
- 连续选择约束（禁非连续，避免时间空洞段落）

### 提取为新小节
- 左树多选若干段落→菜单"提取为新小节"→自动建新小节（当前小节的子级）并装入（决策4b）

### 合并（独立操作，补删除迁移的反向缺口）
- **选区约束**：必须 **同级 + 同父 + 相邻 + 不混级**。选小节合并就不能再带段落，章节同理；跨父同级也不允许
- **存活者**：合并触发后浮现选择气泡"保留哪个？[节点A标题(类型)] [节点B标题(类型)]"，点选→保留该节点标题+类型，另一个标题/类型丢弃、句子/子节点按时间序吸收
- **绝不引起时间冲突**：在"结构=时间线"不变量下，相邻同级兄弟时间范围本就连续，合并=并集相邻时间范围，天然无冲突
- 结果时间范围自动重算（决策4e）
- 段落合并：两句群按时间序拼接；小节/章节合并：子节点全部并入，层级不变

### reparent（拖拽重排）
- 拖到节点上=成为其子级；拖到上方/下方=同级前后移（决策4b）
- **受时间线约束**：只允许时间线一致的移动（如修 AI 误分组，内容时间本就在目标范围内）；**跨时间区域移动被拒绝**
- 非法拖拽：拒绝放置 + 提示"该位置与时间线不符，无法移动；跨时间关联请用笔记引用"
- 可拖整棵子树；段落不能成为段落的父节点；无人工深度上限（决策4f）
- 通用编辑：双击改名为右键改名（见交互节）；段落类型可下拉改

## 删除规则（决策15）

- **永不删除内容**（核心原则）。节点可删，但删前内容必须迁移
- 删节点 = 内容自动并到上一级兄弟：段落删→句子并上一段；小节删→其下段落并上一小节；章节删→其下小节并上一章节
- **章节删除加确认**（显示"X 个小节将并入上一章节"）；段落/小节自动迁移无确认
- **首个节点不可删（仅约束 delete）**：每个父级下的第一个子节点（无前序兄弟）且非空 → delete 置灰禁用 + hover 提示"首个节点不可删除；如需去除请用合并，或清空后删空容器"
- **合并可绕过保护**：首个节点能经"合并"消失（合并是双向显式选择，安全）；"首个不可删"只约束 delete 动作
- **空容器直接删**（无内容/无子节点，含空的首个容器，不违反原则）
- **孤节点往上走一级**：某节只有一段（孤首段，无兄弟）→ delete/合并都不能直接去 → 改为**合并该节**（与相邻小节合并，段落随小节并入）；某章只一节一段→**合并该章**。唯一去不掉：整个视频只有一章一节一段（=全部内容，本就不该删）
- 恢复方式 = 再拆
- 笔记引用句子（sentenceIds），删段落时内容迁移、引用仍指向句子，无级联问题（决策18）
- 结构编辑全集：拆分、reparent、重命名、改类型、删除（内容迁移）、**合并**
- 原 M05-3"删除节点时摘注/随记级联规则"已作废
