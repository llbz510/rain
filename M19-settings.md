# M19 — 设置与模型管理

> 状态：已确认（第九次会话补"模型导入与选择"交互，决策82）

## 三种模型分别可选

用户独立选择三种用途的模型：

| 模型类型 | 用途 | 要求 | 说明 |
|---------|------|------|------|
| ASR 模型 | 语音转文字（M04 Stage 1） | 语音识别 | 三档：字幕/API/本地Whisper，选具体模型 |
| 结构化 LLM | 文本结构化（M04 Stage 2） | 纯文本，不需要 vision | 通用 LLM，支持 structured output |
| 助手 LLM | AI 助手问答（M10） | **必须 vision-capable** | 用于自由对话 + 快捷操作 + 画面理解 |

## ASR 模型配置

### 三档

| 档位 | 说明 | 配置项 |
|------|------|--------|
| 字幕 | 使用视频已有字幕 | 无需配置（自动检测） |
| API | 云端 ASR API | 选供应商 + API Key |
| 本地Whisper | 本地 Whisper 模型 | 选模型大小 |

### API 档供应商

- **国内供应商**：预置常见国内 ASR 服务（阿里云/腾讯云/讯飞等）
- **自定义供应商**：用户填写 API endpoint + API Key + 模型名，支持任意供应商

### 本地 Whisper 档

- 模型大小可选：tiny / base / small / medium / large-v3
- 首次使用需下载模型
- 在 Tauri Rust 后端运行
- 运行后端偏好可选：`自动（推荐）` / `NVIDIA GPU` / `CPU`
- 默认 `自动`：CUDA worker 可用且资源满足时优先 GPU，否则显示原因并回退 CPU
- 显式 `NVIDIA GPU` 不静默回退；无 NVIDIA/CUDA 时应用仍可启动并允许切回 CPU

## 结构化 LLM 配置

- 供应商 + API Key + 模型名
- 支持 OpenAI 兼容接口的任意供应商
- 需支持 structured output / function calling（用于 JSON 输出约束）

## 助手 LLM 配置

- 供应商 + API Key + 模型名
- **必须 vision-capable**（GPT-4o / Claude 3.5 / Gemini / 国内 vision 模型等）
- 支持 OpenAI 兼容接口的任意供应商

## 配置存储

- API Key **不加密**，明文存 SQLite `setting` 表（决策84；键名 `api_key.<模型别名>`）
- 不上传任何服务器
- 详见 M15

## 模型准入约束（M18 讨论新增）

| 模型类型 | 准入约束 | 原因 |
|---------|---------|------|
| 结构化 LLM | 必须允许**大输出**（不只看上下文窗口，还要看单次最大输出 token） | Stage 2 要输出整棵段落树 JSON，输出上限太小的模型（如 4K）做不了结构化 |
| ASR API 供应商 | 必须**返回句级时间戳** | M07 双击句子跳转、M02 Sentence[] 依赖它；只给纯文本的供应商会让逐句跳转失效 |
| ASR API 供应商 | **支持长音频**（讯飞长语音/阿里/腾讯等） | 长视频需零切块；不支持长音频的供应商（如 OpenAI Whisper API ~30min）长视频直接失败，不兜底 |
| ASR API 供应商 | 须**支持自动语言检测** | 决策85：三档 ASR 纯自动检测，用户不选语言 |
| 结构化分块阈值 | 高级设置可调，默认 33%（窗口占比） | 不同模型窗口不同；用户可手动调（见 M18 触发） |

> 选型建议：结构化 LLM 优先选大上下文模型（200K+），长视频越不容易触发分块、质量风险越小。

## 模型导入与选择（第九次会话补，决策82，经原型验证）

### 整体模式：模型池 + 角色选择
- 设置页"模型管理"：用户先把模型**添加进池**（命名保存），再给三个角色各从池里选一个当前用。切换模型不重填配置。
- 角色选择区：ASR / 结构化 LLM / 助手 LLM 三个下拉，从池里选。

### 添加模型（3 类 + 本地）
- **API 类**（结构化 LLM / 助手 LLM / ASR-API）：表单 = 供应商（预置下拉：阿里/腾讯/讯飞/OpenAI 兼容，或"自定义"）+ baseURL（仅自定义显示）+ API Key + 模型名 + 别名。保存成池条目。
- **本地 Whisper**：单条目，模型大小下拉可切（tiny…large-v3）；首次使用触发下载（显进度），下完入池。
- **字幕档**：无需模型，ASR 角色直接选"用视频字幕"。

### 角色选择规则
- ASR = 字幕 / 池里 API-ASR / 池里本地 Whisper，三选一。
- 结构化 LLM = 从池里 LLM 条目下拉选。
- 助手 LLM = 从池里**只列勾了 vision 的** LLM 条目选。

### 测试与 vision
- 每个池条目有"测试"按钮（发简单请求验连通/Key）。
- 添加 LLM 模型时用户手动勾"支持画面(vision)"（自动探测不可靠）；助手角色只列勾选项。

> 原型：`prototype/m19-settings-mockup.html`（角色选择 + 模型池 + 添加模型弹窗，类型切换/供应商预置+自定义/vision 勾选均可操作）。
