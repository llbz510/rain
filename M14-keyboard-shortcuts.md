# M14 — 快捷键

> 状态：已确认（v4.3，第六次会话）

## 总则

- **非输入态**：以下全局快捷键生效。焦点不在输入框时，视频控制类键直接作用于视频。
- **输入态**（AI/随记输入框聚焦）：全局快捷键禁用，键入正常；`Enter`=发送、`Alt+Enter`=换行。
- **树/导图导航 = 鼠标**：不设键盘导航（单击选/双击跳/drag scrub 均鼠标）。键盘编辑仅 `Del`。
- **AI快捷操作 = 鼠标点按**：不设快捷键（操作按段落类型差异化、最多7个、助记难）。

## 快捷键表

### 全局（非输入态）

| 键 | 功能 | 说明 |
|----|------|------|
| `1`/`2`/`3` | 模式切换 | 随播 / 文本展开 / 目录展开（替代渐构原映射） |
| `` ` `` | 摘注播放段 | 摘注当前播放位置所在段落（暂停时=暂停处）；浏览时摘指定段用段落头摘注按钮 |
| `Space` | 播放/暂停 | |
| `←`/`→` | ±5s seek | |
| `Shift+←`/`Shift+→` | ±10s seek | |
| `↑`/`↓` | 音量增/减 | |
| `N`/`P` | 下一段落/上一段落起点 | 结构化 seek：跳到时间序下一/上一段落起点（seek 视频 + 选中该段 + 预览更新）。首段 P/末段 N = no-op |
| `Del`/`Backspace` | 删除选中节点 | 按决策15（段落/小节自动迁移、章节弹确认、首个不可删）；**仅左树选中生效**（见下） |
| `Tab` | 切 AI↔随记面板 | 焦点跟到切换后面板的输入框；从非输入态按 Tab 进 AI 输入 |

### 输入框内（AI/随记）

| 键 | 功能 |
|----|------|
| `Enter` | 发送 |
| `Alt+Enter` | 换行 |
| 其他键 | 正常输入（全局快捷键禁用） |

## `Del` 选中来源约束（与决策47/48 交叉）

- 选中节点**视觉全局共享**（决策48：左树/导图单击 → 两边高亮同一节点）——不变。
- 但 `Del` **仅在选中来源 = 左树时生效**；导图选中的节点按 `Del` 无反应（保决策47"导图纯看"，连键盘删除也不从导图触发）。
- 实现：store 在 `selectedNodeId` 外加 `selectionOrigin: 'tree'|'diagram'`；`Del` 仅 `origin==='tree'` 触发。
- 删除的另一路径 = 左树右键菜单（鼠标，决策38），亦仅左树。两条删除路径都左树作用域，一致。
- 重命名/改类型/合并/提取新小节/reparent 均无键盘快捷键，走左树右键菜单（鼠标）。

## 鼠标负责（无快捷键）

- 树/导图导航：单击选 / 双击跳（N3）/ 导图框内拖 scrub（决策50）
- 树编辑：右键菜单（重命名/改类型/删除/提取新小节/合并/reparent，决策38-46）
- AI快捷操作：点按（M10）
- 文本区滚动：滚轮
- 摘注按钮：段落头（摘指定段，M08）
- 面板切换：点 Tab 头（亦可 `Tab` 键）

## 与渐构差异

| 项 | 渐构 | Rain |
|----|------|------|
| 模式切换 | `1`切换目录区/`2`回随播/`3`切换文本区 | `1`/`2`/`3` 直接对应三模式（随播/文本展开/目录展开） |
| 摘注 `` ` `` | 摘注"当前段落"（未明确） | 明确=播放段（暂停处）；指定段用按钮 |
| 跳段落 | — | 新增 `N`/`P` 结构化 seek |
| 粗 seek | — | 新增 `Shift+←/→` ±10s |
| 切面板 | — | 新增 `Tab` |
| 删节点 | — | 新增 `Del`（仅左树选中） |
| 树/导图键盘导航 | 有 | **无**（鼠标操作） |
| AI操作快捷键 | — | **无**（点按） |
