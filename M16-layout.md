# M16 — 界面布局

> 状态：已确认（v4.5，第八次会话修订控制栏位置）

## 四区结构

| 区域 | 说明 | 模块 |
|------|------|------|
| 左侧目录树 | 竖状章节/小节/段落完整三级导航（树形，可展开折叠），兼结构编辑面+双击跳转面；**全高（顶到底）** | M05 |
| 中间主区域 | 顶部目录横条 + 视频/文本/导图（按模式切换）+ 控制栏 | M05/M06/M07 |
| 右侧面板 | AI助手 / 随记，Tab 切换；**全高（顶到底）** | M08/M10 |
| 视频控制栏 | 随播模式收进视频区内；始终压在文本区正上方；含进度条/暂停继续/音量/设置/文本展开/导图展开/全屏；设置内含字幕开关+译文开关（决策91） | M06 |

> 左树与右面板在三种模式下均贯穿顶到底、不被控制栏截断。控制栏只在中间列，不跨左树/右面板。

## 三种模式

### ① 随播模式（默认，看视频时）

```
┌─────────┬──────────────────────────┬───────────┐
│         │  目录横条（章节>小节/段落）  │           │
│         ├──────────────────────────┤           │
│ 竖状    │         视频画面           │  AI助手    │
│ 章节    │       (视频+ASR字幕)      │  /随记     │
│ 目录树  ├──────────────────────────┤  Tab切换   │
│ (左侧   │  控制栏[▶ ━━━ 音量 设置 展开 全屏]│      │
│  全高)  ├──────────────────────────┤  (全高)    │
│         │  文本区（滚动转录稿）       │           │
└─────────┴──────────────────────────┴───────────┘
```

- **左侧目录树**：全高；章节/小节/段落完整三级，树形可展开/折叠；单击选中、双击三区跳转、右键编辑（详见 M05）
- **顶部目录横条**（M05）：左半=章节群，右半=当前章节的小节群；底部行=当前小节的段落。进度填充，横向滚动
- **视频区**：播放原视频 + ASR逐句字幕（标准CC样式，叠加画面）；**底部收一条控制栏**
- **控制栏**：进度条 / 暂停·继续 / 音量 / 设置（倍速·清晰度·字幕开关）/ 文本展开 / 导图展开 / 全屏
- **文本区**：滚动转录稿，显示连续多段文本，当前句子高亮，自动滚动跟随视频进度。双击句子跳转视频。段落类型标签可见
- **右侧面板**：AI助手 / 随记，Tab切换

### ② 文本展开模式（回顾文本时）

```
┌─────────┬──────────────────────────┬───────────┐
│         │  目录横条                  │           │
│         ├──────────────────────────┤           │
│ 竖状    │  控制栏[▶ ━━━ 音量 设置 收起 全屏]│       │
│ 章节    ├──────────────────────────┤  AI助手    │
│ 目录树  │  文本区（全文稿，填满到底）   │  /随记     │
│ (左侧   │  [章节>小节>段落 带标题]    │  Tab切换   │
│  全高)  │  [双击句子跳转]            │  (全高)    │
└─────────┴──────────────────────────┴───────────┘
```

- **左侧目录树**：全高，仍在
- **顶部目录横条**：仍在
- **控制栏**：**上滑到目录横条正下方**（视频收起，控制栏从视频区底部上移到顶部）
- **文本区**：展开为全文稿，从控制栏下方填满到中间列底部；按章节>小节>段落组织，带各级标题；当前段落/句子背景高亮定位；双击句子仍跳转视频
- **视频区**：收起（不显示画面），其位置被文本区接管
- **右侧面板**：不变

### ③ 目录展开模式（看整体结构时）

```
┌─────────┬──────────────────────────┬───────────┐
│         │  导图区（水平树形导图）     │           │
│ 竖状    │  [根→章节→小节→段落]      │  AI助手    │
│ 章节    │  向右展开，横向滚动        │  /随记     │
│ 目录树  ├──────────────────────────┤  Tab切换   │
│ (左侧   │  控制栏[▶ ━━━ 音量 设置 收起 全屏]│      │
│  全高)  ├──────────────────────────┤  (全高)    │
│         │  文本预览（选中节点段落）    │           │
└─────────┴──────────────────────────┴───────────┘
```

- **左侧目录树**：全高，仍在（目录展开只占据视频区域，不接管左侧目录树）
- **导图区**：原目录横条+视频区位置 → 水平树形导图（根在左向右展开，章节>小节>段落）。默认全展开，可折叠子树。交互见 M05"导图区"
- **控制栏**：**位置不变**（仍压在文本上方，即导图区与文本预览之间）
- **文本预览**：底部显示选中节点的最早叶子段落文本；粘性跟选中、不跟播放；进模式时选中初始化=当前播放节点（见 M05"导图区"）
- **视频区**：收起为控制栏（不显示画面）
- **右侧面板**：不变

## 控制栏不变量

- 控制栏**始终直接压在文本区（文本区/文本预览）正上方**。
- 随播、目录展开：控制栏上方有视觉区（视频画面/导图），控制栏夹在视觉区与文本之间。
- 文本展开：无视觉区（视频收起），控制栏上滑到目录横条正下方，文本区填满其下到底。
- 控制栏只在中间列，不跨左树/右面板。

## 控制栏内容

- 进度条 / 暂停·继续 / 音量 / 设置（倍速·清晰度·字幕开关·**译文开关**）/ 文本展开 / 导图展开 / 全屏

> **字幕开关 vs 译文开关（决策91）**：两个独立开关。「字幕」控制视频区 ASR 字幕显隐（所有视频显示）；「译文」控制文本区翻译块显隐（仅英文视频显示，见 M07 决策86）。两者互不影响。
- "文本展开""导图展开"为模式切换按钮（见"模式切换"）
- 返回列表（M17 决策54）：在顶栏左上角，不在控制栏

## 模式切换

- 控制栏内"文本展开""导图展开"两按钮：点击进入对应展开模式，按钮变"文本收起"/"导图收起"
- 同一时间只开一个展开模式：开着一个时点另一个 → 自动收起当前的，展开新的
- 点"文本收起"/"导图收起" → 回随播模式

## 尺寸比例

- 用户可拖拽分隔线调整，记忆比例
- **默认比例**：
  - 顶部目录横条：固定高度（约 80px，两条横条）
  - 左侧目录树 : 中间主区域 : 右侧面板 ≈ 1 : 7 : 3
  - 随播模式下视频区（含底部控制栏）: 文本区 = 6 : 4

## 视频字幕

- ASR 生成的逐句字幕，叠加在视频画面上（标准 CC 样式）
- 不是原视频的字幕轨（很多视频无字幕或质量差）
- 字幕与文本区是分开的两个东西：字幕=逐句叠加在视频上；文本区=结构化滚动转录稿

## 右侧面板

- AI助手面板 / 随记面板，Tab 切换
- 同一时间只显示一个面板，占满右侧宽度
- AI 回答可手动"存入随记"→ 切到随记 Tab 查看