# M11 — 派生系统

> 状态：低优先级，v1 不实现，数据模型预留字段

## 定义

当随记中积累的摘注/笔记太多时，用户可通过操作导图赋予它们结构信息，形成"派生"——一个重组的知识结构。

## v1 预留

- 数据模型中 `Excerpt.derivationId` 和 `Note.derivationId` 字段已预留
- v1 不实现派生导图 UI 和交互

## v2 设计方向（借鉴渐构）

- 从随记面板拖拽摘注/笔记到导图
- 拖拽目录区/标签区的标签到导图
- 直接在导图中添加空节点（用于站外引用）
- 悬停派生节点可播放原段落内容
- 派生可命名和添加概述
- 支持多标签栏打开多个视频，方便跨视频整理
