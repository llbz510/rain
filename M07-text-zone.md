# M07 — 文本区

## 核心定义

文本区展示的是**原始转录文本**（不清洗），按段落树结构组织。

- 不含 LaTeX 公式渲染
- 不含代码块渲染
- 不清洗口癖/重复/语法问题
- 公式和代码用户直接看视频区

> **结构=时间线 不变量**（见 M05）：段落树永远等于时间线索引。因此"按段落树结构组织"与"自动滚动跟随视频进度"永远同义，无歧义。reparent 受时间线约束，保证二者不分家。

## 文本区编辑（v4.2 新增）

文本区除展示外，支持**单节点三件套 + 句级拆分**（节点级多选操作仍在左树）：

**段落块结构：**
```
┌─────────────────────────────────────────────┐
│ [概念描述 ▾]  段落标题文字      摘注  ⋯    │ ← 段落头（⋯ 悬停浮现）
│ 句子1文本。句子2文本。句子3文本。           │
└─────────────────────────────────────────────┘
```
- **`[类型 ▾]` 胶囊**：单击→4 种类型下拉（**改类型**快捷路径）
- **`⋯` 更多菜单**（悬停浮现）：重命名 / 改类型 / 删除 / 提取为新小节 / 合并
- **`摘注` 按钮**：单击创建笔记（快捷键 `，见 M08）
- **重命名**：菜单触发→标题原地变输入框，回车确认/Esc 取消/失焦确认
- **删除**：菜单触发→按决策15（段落自动迁移、章节弹确认、首个不可删）
- 章节/小节标题头（文本展开模式可见）同样悬停浮现 `⋯`，菜单只含重命名/删除

**拆分（提取为新段落）：**
- 拖选连续句子（**仅本段内**，跨段边界自动截断）→ 浮现工具栏：`✂ 提取为新段落 | 全选段落 | 复制`
- 提取→新建兄弟段落（类型默认同原），标题输入框自动聚焦，原段落留剩余句子，时间范围自动重算（决策4e）

> 注：`+ 摘注选中句` 不放入工具栏。M08 已定"摘注=整段"语义（决策16），句级引用通过摘注后编辑笔记添加 sentenceIds 实现，不在拆分工具栏内。

## 双模式

### 随播模式（播放时）

- **滚动转录稿**：显示连续多段文本（非单段字幕），当前播放句子高亮
- 自动滚动跟随视频进度，用户也可手动滚动
- 段落类型标签显示在每段左侧
- 双击句子跳转到视频对应时刻
- 公式/代码不在转录稿中渲染（看视频区）
- 与视频字幕分开：视频画面上有 ASR 逐句字幕（CC 样式），文本区是结构化滚动转录稿

### 展开模式（回顾时）

- 文本区接管视频区位置，显示完整结构化转录稿
- 按章节>小节>段落组织，带各级标题
- 当前段落/当前句子通过背景高亮定位
- 段落标题行右侧有摘注按钮（见 M08）
- 视频收起为底部控制栏，双击句子仍可跳转视频

## 逐句时间戳

- 由 ASR 段/词级时间戳后处理映射为句级（见 M04"ASR 输出标准化"）
- **双击句子**跳转到视频对应时刻
- 不清洗文本使逐句映射更准确（原始转录与 ASR 输出一一对应）

## 段落类型标签

- 每个段落显示类型胶囊（概念描述/例子/类比/过渡）
- 胶囊颜色区分类型（具体配色见 M13）
- **单击胶囊→4 种类型下拉**（改类型的快捷路径，见"文本区编辑"节）

## 翻译展示（已确认，决策86）

- 英文视频：段落有 `translation`（中文翻译，段落级一个 string，见 M02/决策33），原文 `text`/`sentences` 保留不变（保时间戳映射）。
- **呈现**：每个英文段落块下方紧贴一块中文翻译（同段落内视觉成组）；随播模式与展开模式同样呈现。
- **不句级高亮**：随播模式英文逐句高亮跟着播放走，中文翻译是整段显示、不做句级同步高亮（段落级粒度，与句子级哲学不冲突——翻译服务整段理解）。
- **控制栏「译」开关**：挨着字幕开关；仅英文视频（有 translation 字段）显示，非英文视频隐藏；**默认开**，用户可一键关。
- **字幕区不显翻译**：视频画面上的 ASR 字幕只显英文原文逐句（M13 决策78），翻译只在文本区。
- 原则：原文与翻译并存、不替换（见 M04 translation 字段）。

## 不做（与渐构的差异）

| 渐构功能 | Rain | 原因 |
|---------|------|------|
| LaTeX 公式渲染 | ❌ | 文本不表达公式，看视频区 |
| 代码块渲染 | ❌ | 同上 |
| 文本清洗 | ❌ | 保留原始转录 |
| 讨论区 | ❌ | 无讨论功能 |
| 讨论热度（🔥） | ❌ | 无讨论功能 |
| 公式点击复制 | ❌ | 无公式渲染 |
| 关联段落闪烁 | v2 再定 | 删除笔记后引用段落是否闪烁，留 v2 |
| 富文本编辑段落 | ❌ | 文本是 AI 产出，不编辑（随记里编辑） |
