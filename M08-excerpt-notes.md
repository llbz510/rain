# M08 — 摘注与随记系统（合并原 M08 + M09）

## 流水线

```
摘注（动作：一键捕获段落）→ 随记面板（可编辑笔记 + 自由笔记）→ 派生（重组导图，M11，v2）
```

摘注是**动作**，不是实体。动作的结果是在随记面板创建一条可编辑笔记。详见 M02 Note 实体。

## 摘注（Excerpt 动作）

### 定义

一键捕获段落。段落标题行右侧有摘注按钮，点击后**在随记面板创建一条可编辑笔记**，该笔记自动关联被摘注的段落。

### 使用方式

- **一键摘抄**：点击摘注按钮（或按 ` 键）→ 随记面板立即出现一条笔记，自动关联当前段落，无需任何输入
- **即时编辑**：笔记内容可编辑，用户可立即写下自己的理解（也可事后补）
- **多段引用**：笔记可继续添加其他段落的句子引用（`sentenceIds` 为数组，可跨段）
- **引用持久**：笔记引用句子（sentenceIds），句子永不删。段落拆分/合并/重排/删除（内容迁移）后，引用仍指向对应句子，永远有效

> 与渐构的差异：渐构区分"只摘抄不注释"和"既摘抄又注释"两种模式。Rain 简化为一个动作（捕获→创建可编辑笔记），写什么是可选的后续行为，因为个人工具可暂停视频，没有时限压力。

### 快捷键

按 ` 键（数字1左侧）摘注当前段落。

## 随记（Notes Panel）

### 位置

右侧面板，AI 助手面板旁边（具体布局见 M16）。

### 内容

随记面板是混合面板，所有笔记统一为 Note 实体，用 `source` 区分来源：

| source | 说明 | 初始状态 |
|--------|------|---------|
| `'excerpt'` | 摘注按钮创建 | content 空，sentenceIds=[被摘注段落的全部句子id] |
| `'user'` | 用户手写笔记 | content 空，sentenceIds=[] |
| `'ai'` | AI 回答手动存入 | content=AI回答，sentenceIds=[关联段落的句子id?] |

三种笔记在面板中统一显示，均可编辑内容、均可添加段落引用。UI 可用小图标区分来源。

### 操作

- 摘注创建的笔记：编辑内容、添加更多段落引用
- 新建自由笔记：手写内容，可绑定段落或视频级
- AI 助手的回答可**手动选择**保存到随记（见 M10）
- 管理和回顾所有笔记
- 点击引用徽章可跳回对应句子（及其当前所在段落）

### 与派生的衔接

- 随记中积累的笔记，未来可拖入派生导图重组（M11，v2）
- 数据模型预留 `derivationId` 字段

## 与渐构的设计差异

| 维度 | 渐构 | Rain |
|------|------|------|
| 系统数量 | 摘注 + 派生（2个） | 摘注+随记 合为1个模块 + 派生（低优先级） |
| 摘注模式 | 两种（只摘抄 / 既摘抄又注释） | 一种（捕获→创建可编辑笔记），内容可选 |
| 时限设计 | 为"有时限"社交场景设计 | 个人工具，可暂停，无时限 |
| 随记 | 无（只有摘注收藏栏） | 混合面板（摘注笔记 + 自由笔记 + AI笔记，统一 Note 实体） |
| 句子引用 | 单引用 | 多引用（sentenceIds 数组，句子永不删，跨结构操作全有效） |
| 派生 | 核心功能 | 低优先级，v1 预留 |

## 删除规则（M05×M08，决策15 更新；完整规则见 M05）

- **永不删除内容**（核心原则）。节点可删，但删前内容必须迁移。
- 删节点 = 内容自动并到上一级兄弟：段落删→句子并上一段；小节删→其下段落并上一小节；章节删→其下小节并上一章节。
- **章节删除加确认**（显示"X 个小节将并入上一章节"），段落/小节自动。
- **首个节点不可删（仅约束 delete）**；合并可绕过保护；空容器直删；孤节点往上走一级合并父容器。详见 M05。
- 恢复方式 = 再拆。
- 不存文本快照：笔记引用句子（sentenceIds），句子永不删，跨合并/拆分/重排/删除（内容迁移）引用全有效，无级联问题（决策18）。
