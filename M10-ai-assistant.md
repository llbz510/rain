# M10 — AI 助手

## 核心架构

**结构化快捷操作 + 自由对话**，两者并存。vision 能力两者都可用。

## 两种模式

### 快捷操作（类型化）

快捷操作**按段落类型差异化**。用户选中一个段落后，AI 助手面板显示该段落类型对应的快捷操作。

#### 概念描述（concept）

| 操作 | 说明 |
|------|------|
| 变简约 | 简化当前段落的表达 |
| 变详细 | 展开更详细的解释 |
| 生成例子 | 生成一个具体例子 |
| 生成类比 | 生成一个类比 |
| 分析输入输出 | 分析概念的输入和输出 |
| 生成概念习题 | 生成针对该概念的练习题 |
| 生成关联习题 | 生成跨概念关联的练习题 |

#### 例子（example）

| 操作 | 说明 |
|------|------|
| 提取概念 | 这个例子在演示什么概念 |
| 换个例子 | 生成另一个例子 |
| 变式练习 | 改变条件看结果变化 |

#### 类比（analogy）

| 操作 | 说明 |
|------|------|
| 解释对应关系 | 类比中什么对应什么 |
| 换个类比 | 生成另一个类比 |
| 指出类比局限 | 这个类比在哪里不成立 |

#### 过渡（transition）

无类型专属操作。

### 通用操作（不区分类型）

| 操作 | 触发级别 | 说明 |
|------|---------|------|
| 解释画面 | 段落级 | 用 vision 看当前视频帧，解释画面中的公式/图表/代码 |
| 总结本节 | 小节级 | 总结当前小节全部段落 |

### 自由对话

- 用户自由提问
- 附带当前视频帧（vision）
- 附带当前段落文本作为上下文

## Vision 能力

### 画面理解策略

**当前帧 + 手动定帧**：

- **默认**：用户在随播模式提问时，自动附带当前播放帧
- **手动覆盖**：用户可暂停、拖到想问的具体画面再提问
- 不做段落代表帧、不做多帧采样

### 技术前提

- 助手 LLM 必须是 vision-capable 模型（用户在 M19 中选择）
- 提问时：截取当前帧 → 转为图片 → 附带在 prompt 中发给 LLM

## 上下文范围

### v1（当前视频内）

| 操作 | AI 拿到的上下文 |
|------|---------------|
| 段落级操作 | 当前段落文本 + 当前视频帧 |
| 小节级操作 | 本节全部段落文本 |
| 自由对话 | 当前段落文本 + 当前视频帧 |

> **英文视频时上下文取原文**（决策86）：`当前段落文本`/`本节全部段落文本` 取 `sentences` 原文，**不含 `translation`**。翻译服务人类阅读、不喂 AI（翻译有损、AI 为 vision 强模型能直接处理英文、省 token）。边缘场景（用户问"这翻译对不对"）由用户把翻译贴进自由对话。

### v2（跨视频）

- 跨视频关联搜索（需要 embedding + 向量库）
- 留待 v2

## 输出与随记

- AI 回答默认显示在 AI 助手对话面板
- 回答下方有"保存到随记"按钮
- **用户手动选择**是否保存，不自动保存
- 保存到随记后，可作为自由笔记或段落注释
- 打通链路：`AI助手回答 → 手动存随记 → 积累后拖入派生`

## 不做

- 每日配额限制
- 自动保存 AI 回答到随记
- 跨视频搜索（v2）
- 段落文本编辑（AI 产出的段落文本不可编辑，用户在随记里编辑自己的笔记）
