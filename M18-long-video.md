# M18 — 长视频分段处理

> 状态：✅ 已确认（第四会话）

## 定位

M18 是 **Stage 2（LLM 结构化）的包装层**，仅在转录文本超出结构化 LLM 上下文预算时启用。

```
短视频：转录文本 ──→ Stage 2（单次调用）──→ 完整 JSON
长视频：转录文本 ──→ [M18 包装层]
                       ├─ ① 分块
                       ├─ ② 每块各跑一次 Stage 2（契约不变）
                       └─ ③ 合并局部树 → 完整 JSON
```

- Stage 2 每次调用契约不变（输入一段转录文本块，输出一棵局部段落树）。
- 合并是 M18 独有的第三步，不在 Stage 2 内。
- 决策2 措辞更新为「单次调用（短）/ 分块多次调用（长，由 M18 包装）」。

## 长视频挑战分两层

| 层 | 问题 | 归属 |
|----|------|------|
| Stage 1 ASR | 音频太长 | M04（见下"Stage 1 ASR 长音频"） |
| Stage 2 结构化 | 文本太长 | M18（本文档） |

M18 聚焦 Stage 2 文本分块；Stage 1 ASR 长音频处理见 M04，本文档引用之。

## 触发（Q2）

**token-based，模型感知，自动判断。**

- 触发时机：ASR 完成、Stage 2 之前。此时已有完整 Sentence 流，可精确数 token。
- 规则：若转录文本 token 数 > 所选结构化 LLM 上下文窗口 × **33%** → 触发分块。
- 33% 来源（Path B）：M04 输出去掉冗余 `text` 字段（只存 `sentences`），输出 ≈ 输入的 1.5~2 倍；输入 + 输出 ≤ 窗口 → 输入 ≤ ~33%。
- 安全系数（33%）可在高级设置手动调整；默认 33%。

## 分块策略（Q3）

**token 预算制 + 句子边界切割，不额外调 LLM。**

- 每块目标 ≈ 窗口的 **25%**（留足该块自己的输出 + prompt 余量）。
- 累计句子到 ~25% 就在**下一个句子边界**切（绝不切在句子中间，保时间戳完整）。
- 不在分块阶段做语义对齐（不额外跑 LLM/embedding 找话题边界）。
- 不做轻量语义优化（v1）。
- 跨块被切开的概念 → 由 Q4 上下文传递预防 + Q5/Q6 合并缝合。

否决方向：按已有字幕段落（碎片太小，上千次调用）；按语义切分（块大小不可控、违背 token 预算、需额外调用）。

## 每块 Stage 2 契约 + 上下文传递（Q4）

每块 Stage 2 调用的输入：

```
[前情] 所有前置块的 标题(章节/小节/段落) + 上一块的末段(标题+类型+原文+endTime)
[本体] 该块的 Sentence 流（~25% 窗口）
```

- 前情由代码从已处理块的输出抽取（标题 + 末段），累积成下一块前情。**纯代码、零额外 LLM 调用。**
- 该块只对自己的句子做分段；前情仅供连贯判断（章节续/新开、术语一致、边界概念续接）。
- 不重叠（overlap）：不重复处理句子，不产生重复段落，合并无需去重。
- 前情体积 ≈ 2K token，加到 ~25% 窗口上仍远低于 33% 触发线。

## 合并（Q5 + Q6）

**合并 = LLM 调用，但只看紧凑元数据、不重读全文。**

合并 LLM 的输入（紧凑、有界）：
- 所有块的 章节/小节/段落 **标题**
- 段落**类型**
- **边界段落**（块_i 末段 + 块_{i+1} 首段）
- **术语表**（从各块抽取）

合并 LLM 不重读完整原文/句子（一直躺在库里不动）。产出**统一方案**：
- 章节合并/重命名（块2 该不该接着块1 的章节）
- 术语/翻译对齐映射
- 边界缝合决策

**代码**把统一方案应用到库里的完整树 → 最终统一树。

体积验算：2 小时分 4 块，元数据合计 ≈ 4-5K token，远低于窗口；合并体积与视频时长基本脱钩，不爆。

**能力边界：** 合并能全局统一结构（章节/术语/命名），但无法重做单段语义（重新分类/分段）——那需要读全文。块内因上下文不足的误分类是残余瑕疵，靠 Q4 预防 + 决策15 手动改补救。

## 溢出对策（运行时）

阈值是估算，运行时仍可能爆。三层防御：

1. **自动重切自愈**：单块 Stage 2 调用失败 / 返回 JSON 校验不完整 → 把该块对半切、重试，最多 N 次。其他块不受影响，已完成进度不丢。
2. **静默截断检测**：每块返回后强制校验 JSON 完整性（括号闭合、字段齐全）。不完整 = 当溢出，走层 1。
3. **手动兜底**：重试 N 次仍失败 → 暂停、报错、提示调低阈值。用户在高级设置调低后，重跑**失败的那块**（不重跑整个视频）。

## 合并阶段失败处理（决策90，补决策29 缺口）

溢出对策三层针对的是 Stage 2 块；合并那次 LLM 调用失败/截断单独处理：

- **不影响已入库的各块结构**：各块 Stage 2 已完成并持久化（块级 JSON 已落库），合并失败不会丢块
- **标 failed 可重试**：视频标 failed + 提示"结构合并失败，可重试"。重试只重跑合并那一次（不重跑各块）
- **可跳过合并直接用**：用户可选"跳过合并"以分块结果直接进学习界面（各块独立树、未统一层级），进界面后靠决策15 手动改
- 与决策89 一致：cancelled/中断恢复走同一套 `video.stage` 逻辑，合并阶段失败 = `stage=merging` 的中断态

## 进度展示（Q7）

**粗状态 + 详细进度对象，两处显示。**

- 粗状态 `Video.status`（pending/processing/ready/failed，不变）：视频列表（M17）用，processing 带小进度条。
- 详细进度对象（新增，导入对话框用）：
  ```
  { stage: 'asr'|'normalizing'|'chunking'|'structuring'|'merging',
    blockCurrent, blockTotal, percent, retrying }
  ```
- ETA：仅结构化阶段给粗略值（N × 单块平均时长，分块后才知道 N）；ASR 不给（GPU/异步，估不准）。
- 自动重切：进度条小字"重试中"，不惊扰；仅重试耗尽才报错。
- M03 长视频预警：导入时、ASR 前，按时长给一句"长视频，处理较久"（token 不可得时的时长兜底提示）。

## 手动调整分块（Q8）

**不做。** 分块是临时加工件，合并完即消失，用户不接触。用户可控的杠杆：阈值可调（高级设置）、树可编辑（决策15）。

## Stage 1 ASR 长音频（见 M04，此处引用）

- 本地 Whisper：内部 30 秒 VAD 自切，原生处理长音频，GPU 下速度可接受。无硬性时长上限。
- API 档：用支持长音频的国产供应商（讯飞长语音/阿里/腾讯等），零切块。
- **不建 ASR 音频切块兜底（永久）**：不支持的供应商（如 OpenAI Whisper API ~30 分钟上限）用于长视频直接失败报错。
- M19 准入约束：API 供应商必须返回句级时间戳。
- 本地 Whisper 句级：词级时间戳 + 按句末标点分组得 Sentence。
- M04 增加「ASR 输出标准化为 Sentence[]」步骤（覆盖字幕档合并碎片、API 对齐、Whisper 词级+标点）。

## 中断恢复（指引，归属 M15）

- 只**原子存 ASR 结果**（完整或不存在，绝不存一半）。
- 中断重开：ASR 结果在且完整 → 直接用、不重跑；Stage 2 + 合并全部重跑（约 3 分钟，可接受）。
- 加载时校验 ASR 结果，坏了就重跑 ASR。
- v2 可加逐块 checkpoint（校验后才跳过）。

## 牵连的已确认决策变更

| 决策 | 变更 |
|------|------|
| 决策2 | 「单次调用」→「单次调用（短）/ 分块多次调用（长，由 M18 包装）」 |
| 决策15 | 「永不删除节点」→「永不删除内容」（节点可删，内容迁移；详见决策15 更新） |
| 决策18 | 笔记引用改「句子级」（sentenceIds），非段落级；详见决策18 更新 |
| 决策20 | 左侧目录树含完整三级（章节/小节/段落），非仅章节/小节 |
| M04 | 输出去掉 `text`（Path B）、新增 `translation`、新增 ASR 标准化步骤、ASR 长音频说明 |
| M07 | 「ASR 天然给出句级时间戳」措辞修正 |
| M19 | 新增模型准入约束（结构化 LLM 输出上限、API 长音频、API 句级时间戳） |
| M15 | 新增中断恢复指引（只存 ASR、其余重跑） |

## 待后续讨论（不在 M18 内）

- **「结构编辑与导航交互」cluster**：N1 目录区随结构编辑实时更新、N2 节点修改交互（删/拆/重排/改名/改类型/合并，在左侧目录树执行）、N3 双击左侧目录树节点→视频+文本+目录三区跳转、决策20 改动细节、单击 vs 双击语义、编辑如何传播到目录。
