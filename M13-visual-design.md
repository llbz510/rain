# M13 — 视觉设计规范

> 状态：已确认（第九次会话，2026-06-28）
> 配色/排版/令牌/列表/专项视觉全部经原型验证定稿。

## 已确认决策（第八次会话，2026-06-27）

### 决策63 主题 = 暗色单主题
- 仅暗色一套，不做亮色、不做跟随系统切换。

### 决策64 中性色阶 = 借渐构四档
- 主背景近黑略带蓝 / 面板深灰 / 分隔线极淡灰 / 文字白·浅灰两级。

### 决策65 无品牌强调色；控件纯灰白
- 选中/主按钮/进度/链接/聚焦不靠色相，靠明暗与字重。
- 聚焦圈=浅灰白2px虚线描边；主按钮=描边款；链接=下划线+浅字；模式按钮激活态=底色变暗。

### 决策66 段落四类 = 全界面唯一颜色（完整映射）
- 概念描述=蓝 / 例子=绿 / 类比=橙 / 过渡=灰（无快捷操作，最宜中性）。

### 决策67 选中态 = 变暗
- 底色明显变暗 + 字体只微暗（保可读）。适用一切选中态。

### 决策68 当前播放 = 变亮；与选中同现 = 暗底叠白亮条
- 播放=白细亮条+极淡底；选中=变暗；同现=暗底叠白亮条。
- 澄清 M05"明暗区分当前/同级"：当前(播放)=亮，同级=默认，选中=更暗第三态。

### 决策69 状态色独立于强调色（列表步细定）
- 失败=红 / 处理中=黄 / 排队=灰；属语义状态，不算强调色。

### 决策70 ■/□ 进度填充色 + 节点文字色
- 段落节点：■=类型色（类型与进度合一），□=类型色极淡底；文字=白色。
- 容器节点（章节/小节）：■=中性，□=中性极淡底；文字白色。
- 化解 M05"目录节点三信号打架"。

### 决策71 文本区类型胶囊 = 保留类型色字
- 文本区 `[类型 ▾]` 小胶囊=类型色字+类型色淡底+类型色细边（小标签，彩字直观）。
- 区别于目录横条段落节点（决策70白字、类型靠■填充）：载体不同，处理不同。

### 决策72 列表卡片视觉（部分，细节续定）
- 卡片=缩略图网格（缩略图主视觉+底部信息条）。
- 就绪卡进度条=中性白/浅灰（列表无段落类型上下文，不用类型色）；已看完=进度条满+淡✓。
- 非就绪卡=缩略图蒙层+状态徽章（失败红/处理中黄/排队灰，决策69落地）。
- 细节已由决策77 定（响应式网格 minmax240 / 缩略图 16:9 / 语言·来源右下角标 / 已看完=信息条文字 / 空状态）。

### 决策73 字体族 = 系统兜底无衬线单族
- 全局单一无衬线字体族，不混搭衬线/显示字体。
- 中文：苹方（macOS）/ 微软雅黑（Windows）/ 系统默认兜底（Linux）。
- 英文与数字：跟随系统无衬线，与中文同族搭配。
- 不引等宽字体族；时间码/进度数字走"等宽数字特性"对齐，宽度不抖。
- 字重三档：常规 400 / 中等 600 / 粗体 700（极少）；不用细体（暗色底辨识差）。

### 决策74 字号阶梯 + 字重映射（经模板验证）
- 5 档阶梯，中文最低 12px：大标题 18 / 区标题 16 / 正文 14 / 次正文 13 / 辅助 12。
- 正文 14（阅读主体，暗色底中文稳）；字幕暂定 16，留专项视觉细化。
- 字重：400 默认；600 用于标题与激活态文字（顶栏标题/卡片标题/Tab 激活）；700 仅 logo 与空状态大标题。选中态不靠字重（决策 67）。

### 决策75 行距（经模板验证）
- 仅管"会换行的阅读文本"；单行控件用固定高度+内边距，不设行距。
- 正文（段落正文·句子）1.7；标题 1.3；次正文（目录树·卡片标题·搜索框·AI/随记正文）1.5。
- 右侧窄面板正文走次正文 1.5（窄列更紧），主正文区 1.7。

### 决策76 令牌：间距/圆角/阴影/关键高度（经模板验证）
- 间距：4px 基准，阶梯 4/8/12/16/20/24/32/48，所有内/外边距·间隙只取阶梯值。
- 圆角：5 档 0/4/8/12/胶囊；类型胶囊=8（中）。
- 阴影：默认无；仅浮层一档 `0 4px 16px rgba(0,0,0,.4)` + 半透明描边（模态/下拉/浮动工具栏/提示）。卡片不加阴影，靠面板色差分层。
- 关键高度：顶栏 40 / 控制栏 40 / 目录横条 80（决策 23）。

### 决策77 M17 列表视觉细节（补决策 72 待定项，经模板验证）
- 网格：响应式 `auto-fill, minmax(240px,1fr)`，窗口宽多排、窄少排，卡宽保底 240。
- 缩略图：16:9，高度随卡宽自适应。
- 就绪卡信息布局：缩略图（右下"语言·来源"角标）→ 标题（13，最多 2 行截断）→ 进度条(4)+右侧时长 → 最近学习小字。
- 已看完 = 进度条满 + 信息条"已看完·时间"文字（覆盖决策 72 的"淡✓"）。
- 非就绪卡：缩略图蒙层 + 左上状态徽章（失败红/处理中黄/排队灰）→ 标题 + 状态文字。
- 空状态：居中占满列表区，大标题 18/700"还没有视频" + 副 13/暗"点右上「导入」导入你的第一个视频"；顶栏仅留导入（藏排序/搜索）。

### 决策78 字幕样式（经模板验证）
- 位置：视频底部居中，距底 16，避开控制栏。
- 底框：半透明黑 rgba(0,0,0,.6)，圆角 4，内边距 4/8。
- 文字：白、16（决策 74 暂定）、行距 1.3、字重 400 + 文字阴影增可读（不靠加粗）。
- 最大宽度约 80% 视频宽，长句允许 2 行。
- 内容：仅 ASR 原文逐句；段落级翻译（决策 33）在文本区不在字幕。

### 决策79 导图节点视觉 + 连线 + 缩放拖拽（经模板验证）
- 节点：圆角框 8、内边距 8/12、边框 1px、最小宽 150；段落=类型色淡底+类型色进度填充+白字，容器=中性填充+白字（决策 66/70 落地）；三态=选中变暗/播放白亮条/同现暗底叠白条（决策 67/68）。
- 连线：SVG 路径，正交折线带圆弧弯折（半径 6），2px、30% 白、连续折线；父节点右侧→弯折→子节点左侧。
- 树形：水平树，根在左向右展开，章节>小节>段落，可折叠子树。
- 缩放：滚轮 0.4–2.5 倍（每格 1.12）。平移：空白处左键拖拽（节点上不触发，留 scrub，决策 50）。与决策 50 节点框内 scrub 互补不冲突。

### 决策80 右侧面板 AI 助手对话样式（经模板验证）
- Tab 切换 AI 助手/随记（决策 22）。
- 对话：用户气泡右、AI 气泡左；用户消息可附"当前帧"缩略图条（决策 6 vision）；AI 回答带"存入随记"（决策 10）。
- 快捷操作芯片：按选中段落类型差异化 + 通用（解释画面/总结本节），决策 10 落地。
- 输入框：多行；Enter 发送、Alt+Enter 换行（决策 53）；底部"附带当前帧·点此改定帧"条（决策 6 手动定帧）。

### 决策81 动效统一（经确认）
- 时长三档：快 120ms / 中 200ms / 慢 320ms。
- 缓动：默认 ease-out；大位移（模式切换）用 ease-in-out。
- 映射：选中/悬停/高亮变色=快 120ms；内容横滑切换（决策12）、自动居中滚动（决策13）、折叠子树、模态/浮层=中 200ms；三模式切换（M16）=慢 320ms。
- 进度填充（决策12）：实时跟播放，不动画化（"从左到右"仅视觉方向）。
- 原则：动效仅作状态反馈、不装饰；尊重"减少动效"系统设置（开启则全部瞬切）。

## 待澄清 / 下一步
1. 排版——已定（决策 73-75）。
2. 令牌——已定（决策 76）。
3. M17 列表视觉细节——已定（决策 77）。
4. 专项视觉——已定（决策 78-81）。